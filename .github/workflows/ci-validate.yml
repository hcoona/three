name: CI Validation

on:
  pull_request:
  push:
    branches:
      - main
  schedule:
    - cron: '17 8 * * *'

permissions:
  contents: read
  actions: read

jobs:
  normalize-input:
    name: normalize-input
    runs-on: ubuntu-latest
    permissions:
      contents: read
      actions: read
    outputs:
      request-artifact-name: ${{ steps.refs.outputs.request_artifact_name }}
      plan-artifact-name: ${{ steps.refs.outputs.plan_artifact_name }}
      planner-diagnostics-artifact-name: ${{ steps.refs.outputs.planner_diagnostics_artifact_name }}
      changed-files-snapshot-artifact-name: ${{ steps.refs.outputs.changed_files_snapshot_artifact_name }}
      fact-snapshot-artifact-name: ${{ steps.refs.outputs.fact_snapshot_artifact_name }}
      execution-batch-manifest-artifact-name: ${{ steps.refs.outputs.execution_batch_manifest_artifact_name }}
      aggregate-evidence-manifest-artifact-name: ${{ steps.refs.outputs.aggregate_evidence_manifest_artifact_name }}
      aggregate-summary-artifact-name: ${{ steps.refs.outputs.aggregate_summary_artifact_name }}
      request-artifact-id: ${{ steps.upload-request.outputs.artifact-id }}
      mode: ${{ steps.mode.outputs.mode }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: false
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
      - name: Install uv
        id: setup-uv
        continue-on-error: true
        uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0
        with:
          version: '0.10.9'
      - name: Retry uv installation
        if: ${{ steps.setup-uv.outcome == 'failure' }}
        uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0
        with:
          version: '0.10.9'
      - name: Compute artifact refs
        id: refs
        run: |
          uv run python eng/scripts/workflow_release_control.py ci-validation-artifact-refs \
            --run-id "$GITHUB_RUN_ID" \
            --run-attempt "$GITHUB_RUN_ATTEMPT" \
            --github-output "$GITHUB_OUTPUT"
      - name: Resolve CI mode
        id: mode
        env:
          EVENT_NAME: ${{ github.event_name }}
        run: |
          mode=push
          if [ "$EVENT_NAME" = "pull_request" ]; then
            mode=pull_request
          elif [ "$EVENT_NAME" = "schedule" ]; then
            mode=scheduled_full
          fi
          echo "mode=${mode}" >> "$GITHUB_OUTPUT"
      - name: Write planner-facing request
        env:
          CI_ACTOR: ${{ github.actor }}
          CI_EVENT_NAME: ${{ github.event_name }}
          CI_EVENT_NUMBER: ${{ github.event.pull_request.number || '' }}
          CI_MODE: ${{ steps.mode.outputs.mode }}
          CI_REF: ${{ github.ref }}
          CI_REPOSITORY: ${{ github.repository }}
        run: |
          mkdir -p .three-ci-validation/normalize
          validation_commit_sha="$(git rev-parse HEAD)"
          validation_ref="$CI_REF"
          if [ "$CI_MODE" = "pull_request" ] && [ -n "$CI_EVENT_NUMBER" ]; then
            validation_ref="refs/pull/${CI_EVENT_NUMBER}/head"
          fi
          range_args=()
          if [ "$CI_MODE" != "scheduled_full" ]; then
            uv run python - <<'PY'
          import json
          import os
          import shlex
          import subprocess

          ZERO_SHA = "0" * 40
          OUT = ".three-ci-validation/normalize/range-args.sh"


          def emit(args):
              with open(OUT, "w", encoding="utf-8") as handle:
                  handle.write("range_args=(")
                  for arg in args:
                      handle.write(" " + shlex.quote(str(arg)))
                  handle.write(")\n")


          def unavailable(detail):
              emit(["--range-status", "unavailable", "--range-diagnostic-detail", detail])


          def git(*args):
              return subprocess.run(
                  ["git", *args],
                  check=True,
                  capture_output=True,
                  text=False,
              ).stdout


          def is_sha(value):
              return (
                  isinstance(value, str)
                  and len(value) == 40
                  and value != ZERO_SHA
                  and all(char in "0123456789abcdefABCDEF" for char in value)
              )


          def commit_exists(value):
              if not is_sha(value):
                  return False
              return subprocess.run(
                  ["git", "cat-file", "-e", f"{value}^{{commit}}"],
                  stdout=subprocess.DEVNULL,
                  stderr=subprocess.DEVNULL,
                  check=False,
              ).returncode == 0


          def ensure_commit(value):
              if commit_exists(value):
                  return True
              if not is_sha(value):
                  return False
              subprocess.run(
                  ["git", "fetch", "--no-tags", "--depth=1", "origin", value],
                  stdout=subprocess.DEVNULL,
                  stderr=subprocess.DEVNULL,
                  check=False,
              )
              return commit_exists(value)


          class NonCanonicalChangedPathError(ValueError):
              pass


          def is_canonical_repo_path(value):
              if any(0xD800 <= ord(char) <= 0xDFFF for char in value):
                  return False
              if (
                  value == ""
                  or value.startswith(("/", "./"))
                  or value.endswith("/")
                  or "\\" in value
              ):
                  return False
              return all(part not in {"", ".", ".."} for part in value.split("/"))


          def check_path(value, seen):
              if not is_canonical_repo_path(value) or value in seen:
                  raise NonCanonicalChangedPathError(
                      "non-canonical or duplicated changed path"
                  )
              seen.add(value)
              return value


          def changed_files(base, head):
              raw = git("diff", "--name-status", "--find-renames", "-z", base, head)
              try:
                  fields = raw.decode("utf-8", "strict").split("\0")
              except UnicodeDecodeError as exc:
                  raise NonCanonicalChangedPathError(
                      "non-UTF-8 changed path"
                  ) from exc
              if fields and fields[-1] == "":
                  fields.pop()
              changed = []
              seen = set()
              index = 0
              while index < len(fields):
                  status = fields[index]
                  index += 1
                  if not status:
                      continue
                  kind = status[0]
                  if kind in {"R", "C"}:
                      if index + 1 >= len(fields):
                          raise ValueError("incomplete rename/copy record")
                      changed.append(check_path(fields[index], seen))
                      changed.append(check_path(fields[index + 1], seen))
                      index += 2
                      continue
                  if index >= len(fields):
                      raise ValueError("incomplete changed-file record")
                  changed.append(check_path(fields[index], seen))
                  index += 1
              return sorted(changed)


          def available(base_sha, head_sha, files, base_tip_sha=None):
              args = [
                  "--range-status",
                  "available",
                  "--base-sha",
                  base_sha,
                  "--head-sha",
                  head_sha,
                  "--changed-files-json",
                  json.dumps(files, ensure_ascii=False, separators=(",", ":")),
              ]
              if base_tip_sha is not None:
                  args[4:4] = ["--base-tip-sha", base_tip_sha]
              emit(args)


          try:
              event_path = os.environ["GITHUB_EVENT_PATH"]
              with open(event_path, encoding="utf-8") as handle:
                  event = json.load(handle)
              mode = os.environ["CI_MODE"]
              validation_commit_sha = git("rev-parse", "HEAD").decode().strip()
              if mode == "push":
                  base_sha = event.get("before")
                  head_sha = event.get("after")
                  if not ensure_commit(base_sha) or not ensure_commit(head_sha):
                      unavailable("incomplete")
                  elif validation_commit_sha != head_sha:
                      unavailable("unconfirmed-provenance")
                  else:
                      available(base_sha, head_sha, changed_files(base_sha, head_sha))
              elif mode == "pull_request":
                  pull_request = event.get("pull_request") or {}
                  base_tip_sha = (pull_request.get("base") or {}).get("sha")
                  head_sha = (pull_request.get("head") or {}).get("sha")
                  if not ensure_commit(base_tip_sha) or not ensure_commit(head_sha):
                      unavailable("incomplete")
                  elif validation_commit_sha != head_sha:
                      unavailable("unconfirmed-provenance")
                  else:
                      base_sha = git("merge-base", base_tip_sha, head_sha).decode().strip()
                      if not ensure_commit(base_sha):
                          unavailable("incomplete")
                      else:
                          available(
                              base_sha,
                              head_sha,
                              changed_files(base_sha, head_sha),
                              base_tip_sha=base_tip_sha,
                          )
              else:
                  unavailable("missing")
          except NonCanonicalChangedPathError:
              unavailable("inconsistent")
          except (KeyError, OSError, subprocess.CalledProcessError, UnicodeError, ValueError, json.JSONDecodeError):
              unavailable("incomplete")
          PY
            # shellcheck source=/dev/null
            source .three-ci-validation/normalize/range-args.sh
          fi
          uv run python eng/scripts/workflow_release_control.py write-ci-validation-request \
            --mode "$CI_MODE" \
            --repository "$CI_REPOSITORY" \
            --workflow "CI Validation" \
            --run-id "$GITHUB_RUN_ID" \
            --run-attempt "$GITHUB_RUN_ATTEMPT" \
            --event-name "$CI_EVENT_NAME" \
            --event-number "$CI_EVENT_NUMBER" \
            --actor "$CI_ACTOR" \
            --validation-commit-sha "$validation_commit_sha" \
            --validation-ref "$validation_ref" \
            "${range_args[@]}" \
            --out .three-ci-validation/normalize/ci-validation-request.json
      - name: Upload planner-facing request
        id: upload-request
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.refs.outputs.request_artifact_name }}
          path: .three-ci-validation/normalize/ci-validation-request.json
          include-hidden-files: true
          if-no-files-found: error
          retention-days: 30

  plan:
    name: plan
    needs: normalize-input
    runs-on: ubuntu-latest
    permissions:
      contents: read
      actions: read
    outputs:
      plan-artifact-id: ${{ steps.upload-plan.outputs.artifact-id }}
      changed-files-snapshot-artifact-id: ${{ steps.upload-changed-files.outputs.artifact-id }}
      fact-snapshot-artifact-id: ${{ steps.upload-fact-snapshot.outputs.artifact-id }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: false
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
      - name: Install uv
        id: setup-uv
        continue-on-error: true
        uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0
        with:
          version: '0.10.9'
      - name: Retry uv installation
        if: ${{ steps.setup-uv.outcome == 'failure' }}
        uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0
        with:
          version: '0.10.9'
      - name: Verify request producer boundary
        env:
          GH_TOKEN: ${{ github.token }}
          REQUEST_REF: ci-validation/requests/${{ github.run_id }}/${{ github.run_attempt }}/ci-validation-request.json
          REQUEST_ARTIFACT_ID: ${{ needs.normalize-input.outputs.request-artifact-id }}
        run: |
          uv run python eng/scripts/workflow_release_control.py verify-ci-validation-artifact-boundaries \
            --repository "$GITHUB_REPOSITORY" \
            --workflow "CI Validation" \
            --run-id "$GITHUB_RUN_ID" \
            --run-attempt "$GITHUB_RUN_ATTEMPT" \
            --expected-artifact "{\"artifact-ref\":\"${REQUEST_REF}\",\"artifact-instance-id\":\"${REQUEST_ARTIFACT_ID}\",\"producer-boundary\":\"normalize-input\",\"producer-job\":\"normalize-input\"}"
      - uses: actions/download-artifact@v4
        if: ${{ needs.normalize-input.outputs.request-artifact-id != '' }}
        with:
          artifact-ids: ${{ needs.normalize-input.outputs.request-artifact-id }}
          merge-multiple: true
          path: .three-ci-validation/request
      - name: Run CI validation planner
        run: |
          mkdir -p .three-ci-validation/plan
          observed_commit_sha="$(git rev-parse HEAD)"
          uv run three-workflow-release-planner ci-plan \
            --repo-root . \
            --request .three-ci-validation/request/ci-validation-request.json \
            --expected-run-id "$GITHUB_RUN_ID" \
            --expected-run-attempt "$GITHUB_RUN_ATTEMPT" \
            --observed-commit-sha "$observed_commit_sha" \
            --plan-out .three-ci-validation/plan/validation-plan.json \
            --changed-files-out .three-ci-validation/plan/changed-files.json \
            --fact-snapshot-out .three-ci-validation/plan/fact-snapshot.json \
            --diagnostics-out .three-ci-validation/plan/planner-diagnostics.json
      - name: Upload validation plan
        id: upload-plan
        uses: actions/upload-artifact@v4
        with:
          name: ${{ needs.normalize-input.outputs.plan-artifact-name }}
          path: .three-ci-validation/plan/validation-plan.json
          include-hidden-files: true
          if-no-files-found: error
          retention-days: 30
      - name: Upload planner diagnostics
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: ${{ needs.normalize-input.outputs.planner-diagnostics-artifact-name }}
          path: .three-ci-validation/plan/planner-diagnostics.json
          include-hidden-files: true
          if-no-files-found: ignore
          retention-days: 30
      - name: Upload changed-files snapshot
        id: upload-changed-files
        uses: actions/upload-artifact@v4
        with:
          name: ${{ needs.normalize-input.outputs.changed-files-snapshot-artifact-name }}
          path: .three-ci-validation/plan/changed-files.json
          include-hidden-files: true
          if-no-files-found: ignore
          retention-days: 30
      - name: Upload fact snapshot
        id: upload-fact-snapshot
        uses: actions/upload-artifact@v4
        with:
          name: ${{ needs.normalize-input.outputs.fact-snapshot-artifact-name }}
          path: .three-ci-validation/plan/fact-snapshot.json
          include-hidden-files: true
          if-no-files-found: ignore
          retention-days: 30

  materialize-execution-batches:
    name: materialize-execution-batches
    needs:
      - normalize-input
      - plan
    if: always() && needs.plan.result == 'success'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      actions: read
    outputs:
      has-execution-batches: ${{ steps.materialize.outputs.has_execution_batches }}
      execution-batch-matrix: ${{ steps.materialize.outputs.execution_batch_matrix }}
      execution-batch-layers: ${{ steps.materialize.outputs.execution_batch_layers }}
      has-execution-batch-layer-0: ${{ steps.materialize.outputs.has_execution_batch_layer_0 }}
      execution-batch-layer-0-matrix: ${{ steps.materialize.outputs.execution_batch_layer_0_matrix }}
      has-execution-batch-layer-1: ${{ steps.materialize.outputs.has_execution_batch_layer_1 }}
      execution-batch-layer-1-matrix: ${{ steps.materialize.outputs.execution_batch_layer_1_matrix }}
      has-execution-batch-layer-2: ${{ steps.materialize.outputs.has_execution_batch_layer_2 }}
      execution-batch-layer-2-matrix: ${{ steps.materialize.outputs.execution_batch_layer_2_matrix }}
      execution-batch-manifest-artifact-id: ${{ steps.upload-execution-batch-manifest.outputs.artifact-id }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
          persist-credentials: false
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
      - name: Install uv
        id: setup-uv
        continue-on-error: true
        uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0
        with:
          version: '0.10.9'
      - name: Retry uv installation
        if: ${{ steps.setup-uv.outcome == 'failure' }}
        uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0
        with:
          version: '0.10.9'
      - name: Verify planning artifact producer boundaries
        env:
          GH_TOKEN: ${{ github.token }}
          REQUEST_REF: ci-validation/requests/${{ github.run_id }}/${{ github.run_attempt }}/ci-validation-request.json
          PLAN_REF: ci-validation/planning/${{ github.run_id }}/${{ github.run_attempt }}/validation-plan.json
          CHANGED_FILES_REF: ci-validation/planning/${{ github.run_id }}/${{ github.run_attempt }}/changed-files-snapshot.json
          FACT_SNAPSHOT_REF: ci-validation/planning/${{ github.run_id }}/${{ github.run_attempt }}/fact-snapshot.json
          REQUEST_ARTIFACT_ID: ${{ needs.normalize-input.outputs.request-artifact-id }}
          PLAN_ARTIFACT_ID: ${{ needs.plan.outputs.plan-artifact-id }}
          CHANGED_FILES_ARTIFACT_ID: ${{ needs.plan.outputs.changed-files-snapshot-artifact-id }}
          FACT_SNAPSHOT_ARTIFACT_ID: ${{ needs.plan.outputs.fact-snapshot-artifact-id }}
        run: |
          expected_artifacts=(
            --expected-artifact "{\"artifact-ref\":\"${REQUEST_REF}\",\"artifact-instance-id\":\"${REQUEST_ARTIFACT_ID}\",\"producer-boundary\":\"normalize-input\",\"producer-job\":\"normalize-input\"}"
            --expected-artifact "{\"artifact-ref\":\"${PLAN_REF}\",\"artifact-instance-id\":\"${PLAN_ARTIFACT_ID}\",\"producer-boundary\":\"plan\",\"producer-job\":\"plan\"}"
          )
          if [ -n "$CHANGED_FILES_ARTIFACT_ID" ]; then
            expected_artifacts+=(--expected-artifact "{\"artifact-ref\":\"${CHANGED_FILES_REF}\",\"artifact-instance-id\":\"${CHANGED_FILES_ARTIFACT_ID}\",\"producer-boundary\":\"plan\",\"producer-job\":\"plan\"}")
          fi
          if [ -n "$FACT_SNAPSHOT_ARTIFACT_ID" ]; then
            expected_artifacts+=(--expected-artifact "{\"artifact-ref\":\"${FACT_SNAPSHOT_REF}\",\"artifact-instance-id\":\"${FACT_SNAPSHOT_ARTIFACT_ID}\",\"producer-boundary\":\"plan\",\"producer-job\":\"plan\"}")
          fi
          uv run python eng/scripts/workflow_release_control.py verify-ci-validation-artifact-boundaries \
            --repository "$GITHUB_REPOSITORY" \
            --workflow "CI Validation" \
            --run-id "$GITHUB_RUN_ID" \
            --run-attempt "$GITHUB_RUN_ATTEMPT" \
            "${expected_artifacts[@]}"
      - uses: actions/download-artifact@v4
        if: ${{ needs.normalize-input.outputs.request-artifact-id != '' }}
        with:
          artifact-ids: ${{ needs.normalize-input.outputs.request-artifact-id }}
          merge-multiple: true
          path: .three-ci-validation/request
      - uses: actions/download-artifact@v4
        if: ${{ needs.plan.outputs.plan-artifact-id != '' }}
        with:
          artifact-ids: ${{ needs.plan.outputs.plan-artifact-id }}
          merge-multiple: true
          path: .three-ci-validation/plan
      - name: Download changed-files snapshot
        if: ${{ needs.plan.outputs.changed-files-snapshot-artifact-id != '' }}
        uses: actions/download-artifact@v4
        with:
          artifact-ids: ${{ needs.plan.outputs.changed-files-snapshot-artifact-id }}
          merge-multiple: true
          path: .three-ci-validation/plan
      - name: Download fact snapshot
        if: ${{ needs.plan.outputs.fact-snapshot-artifact-id != '' }}
        uses: actions/download-artifact@v4
        with:
          artifact-ids: ${{ needs.plan.outputs.fact-snapshot-artifact-id }}
          merge-multiple: true
          path: .three-ci-validation/plan
      - name: Materialize execution batches
        id: materialize
        run: |
          mkdir -p .three-ci-validation/materialize
          uv run python eng/scripts/workflow_release_control.py materialize-ci-validation-execution-batches \
            --plan .three-ci-validation/plan/validation-plan.json \
            --request .three-ci-validation/request/ci-validation-request.json \
            --changed-files-snapshot .three-ci-validation/plan/changed-files.json \
            --fact-snapshot .three-ci-validation/plan/fact-snapshot.json \
            --workflow "CI Validation" \
            --execution-job "execution-batch" \
            --non-batch-control-plane-job-count 4 \
            --expected-run-id "$GITHUB_RUN_ID" \
            --expected-run-attempt "$GITHUB_RUN_ATTEMPT" \
            --execution-batch-manifest-out .three-ci-validation/materialize/execution-batch-manifest.json \
            --github-output "$GITHUB_OUTPUT"
      - name: Upload execution-batch manifest
        id: upload-execution-batch-manifest
        uses: actions/upload-artifact@v4
        with:
          name: ${{ needs.normalize-input.outputs.execution-batch-manifest-artifact-name }}
          path: .three-ci-validation/materialize/execution-batch-manifest.json
          include-hidden-files: true
          if-no-files-found: error
          retention-days: 30

  execution-batch-layer-0:
    name: execution-batch-layer-0
    needs:
      - normalize-input
      - plan
      - materialize-execution-batches
    if: always() && needs.materialize-execution-batches.outputs.has-execution-batch-layer-0 == 'true'
    runs-on: ${{ matrix['runner-family'] == 'windows' && 'windows-latest' || 'ubuntu-latest' }}
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.materialize-execution-batches.outputs.execution-batch-layer-0-matrix) }}
    permissions:
      contents: read
      actions: read
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: false
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
      - name: Install uv
        id: setup-uv
        continue-on-error: true
        uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0
        with:
          version: '0.10.9'
      - name: Retry uv installation
        if: ${{ steps.setup-uv.outcome == 'failure' }}
        uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0
        with:
          version: '0.10.9'
      - name: Install Node.js for JavaScript/TypeScript validation
        if: ${{ matrix.ecosystem == 'javascript' || matrix.ecosystem == 'typescript' }}
        uses: actions/setup-node@v4
        with:
          node-version: '24'
      - name: Enable pnpm for JavaScript/TypeScript validation
        if: ${{ matrix.ecosystem == 'javascript' || matrix.ecosystem == 'typescript' }}
        shell: bash
        run: corepack enable pnpm
      - name: Install Ruby for RubyGems validation
        if: ${{ matrix.ecosystem == 'ruby' }}
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.3'
      - uses: actions/download-artifact@v4
        if: ${{ needs.normalize-input.outputs.request-artifact-id != '' }}
        with:
          artifact-ids: ${{ needs.normalize-input.outputs.request-artifact-id }}
          merge-multiple: true
          path: .three-ci-validation/request
      - uses: actions/download-artifact@v4
        if: ${{ needs.plan.outputs.plan-artifact-id != '' }}
        with:
          artifact-ids: ${{ needs.plan.outputs.plan-artifact-id }}
          merge-multiple: true
          path: .three-ci-validation/plan
      - name: Download execution-batch manifest
        if: ${{ needs.materialize-execution-batches.outputs.execution-batch-manifest-artifact-id != '' }}
        uses: actions/download-artifact@v4
        with:
          artifact-ids: ${{ needs.materialize-execution-batches.outputs.execution-batch-manifest-artifact-id }}
          merge-multiple: true
          path: .three-ci-validation/materialize
      - name: Download changed-files snapshot
        if: ${{ needs.plan.outputs.changed-files-snapshot-artifact-id != '' }}
        uses: actions/download-artifact@v4
        with:
          artifact-ids: ${{ needs.plan.outputs.changed-files-snapshot-artifact-id }}
          merge-multiple: true
          path: .three-ci-validation/plan
      - name: Download fact snapshot
        if: ${{ needs.plan.outputs.fact-snapshot-artifact-id != '' }}
        uses: actions/download-artifact@v4
        with:
          artifact-ids: ${{ needs.plan.outputs.fact-snapshot-artifact-id }}
          merge-multiple: true
          path: .three-ci-validation/plan
      - name: Run batch validation commands
        id: validation
        shell: bash
        env:
          MATRIX_BATCH_JSON: ${{ toJson(matrix) }}
        run: |
          mkdir -p .three-ci-validation/work/validation-results
          shopt -s nullglob globstar
          dependency_bundle_args=()
          for bundle in .three-ci-validation/observed-artifacts/**/batch-evidence-bundle.json; do
            dependency_bundle_args+=(--dependency-bundle "$bundle")
          done
          observed_commit_sha="$(git rev-parse HEAD)"
          uv run python eng/scripts/workflow_release_control.py run-ci-validation-batch-commands \
            --plan .three-ci-validation/plan/validation-plan.json \
            --request .three-ci-validation/request/ci-validation-request.json \
            --execution-batch-manifest .three-ci-validation/materialize/execution-batch-manifest.json \
            --changed-files-snapshot .three-ci-validation/plan/changed-files.json \
            --fact-snapshot .three-ci-validation/plan/fact-snapshot.json \
            --expected-run-id "$GITHUB_RUN_ID" \
            --expected-run-attempt "$GITHUB_RUN_ATTEMPT" \
            --observed-artifacts-dir .three-ci-validation/observed-artifacts \
            --observed-commit-sha "$observed_commit_sha" \
            --matrix-row-json "$MATRIX_BATCH_JSON" \
            --repo-root . \
            --result-out-dir .three-ci-validation/work/validation-results \
            "${dependency_bundle_args[@]}" \
            --github-output "$GITHUB_OUTPUT"
      - name: Write batch evidence bundle
        id: bundle
        shell: bash
        env:
          MATRIX_BATCH_JSON: ${{ toJson(matrix) }}
        run: |
          shopt -s nullglob globstar
          validation_result_args=()
          for result in .three-ci-validation/work/validation-results/*.json; do
            validation_result_args+=(--validation-result "$result")
          done
          dependency_bundle_args=()
          for bundle in .three-ci-validation/observed-artifacts/**/batch-evidence-bundle.json; do
            dependency_bundle_args+=(--dependency-bundle "$bundle")
          done
          observed_commit_sha="$(git rev-parse HEAD)"
          uv run python eng/scripts/workflow_release_control.py write-ci-validation-batch-evidence-bundle \
            --plan .three-ci-validation/plan/validation-plan.json \
            --request .three-ci-validation/request/ci-validation-request.json \
            --execution-batch-manifest .three-ci-validation/materialize/execution-batch-manifest.json \
            --changed-files-snapshot .three-ci-validation/plan/changed-files.json \
            --fact-snapshot .three-ci-validation/plan/fact-snapshot.json \
            --matrix-row-json "$MATRIX_BATCH_JSON" \
            --expected-run-id "$GITHUB_RUN_ID" \
            --expected-run-attempt "$GITHUB_RUN_ATTEMPT" \
            --workflow "CI Validation" \
            --job "execution-batch" \
            --observed-artifacts-dir .three-ci-validation/observed-artifacts \
            --observed-commit-sha "$observed_commit_sha" \
            "${validation_result_args[@]}" \
            "${dependency_bundle_args[@]}" \
            --bundle-out .three-ci-validation/work/batch-evidence-bundle.json \
            --github-output "$GITHUB_OUTPUT"
      - name: Upload batch evidence bundle
        id: upload-batch-evidence
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.bundle.outputs.batch_evidence_bundle_artifact_name }}
          path: |
            .three-ci-validation/work/batch-evidence-bundle.json
            .three-ci-validation/work/validation-results/*.json
          include-hidden-files: true
          if-no-files-found: error
          retention-days: 30
  execution-batch-layer-1:
    name: execution-batch-layer-1
    needs:
      - normalize-input
      - plan
      - materialize-execution-batches
      - execution-batch-layer-0
    if: always() && needs.materialize-execution-batches.outputs.has-execution-batch-layer-1 == 'true'
    runs-on: ${{ matrix['runner-family'] == 'windows' && 'windows-latest' || 'ubuntu-latest' }}
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.materialize-execution-batches.outputs.execution-batch-layer-1-matrix) }}
    permissions:
      contents: read
      actions: read
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: false
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
      - name: Install uv
        id: setup-uv
        continue-on-error: true
        uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0
        with:
          version: '0.10.9'
      - name: Retry uv installation
        if: ${{ steps.setup-uv.outcome == 'failure' }}
        uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0
        with:
          version: '0.10.9'
      - name: Install Node.js for JavaScript/TypeScript validation
        if: ${{ matrix.ecosystem == 'javascript' || matrix.ecosystem == 'typescript' }}
        uses: actions/setup-node@v4
        with:
          node-version: '24'
      - name: Enable pnpm for JavaScript/TypeScript validation
        if: ${{ matrix.ecosystem == 'javascript' || matrix.ecosystem == 'typescript' }}
        shell: bash
        run: corepack enable pnpm
      - name: Install Ruby for RubyGems validation
        if: ${{ matrix.ecosystem == 'ruby' }}
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.3'
      - uses: actions/download-artifact@v4
        if: ${{ needs.normalize-input.outputs.request-artifact-id != '' }}
        with:
          artifact-ids: ${{ needs.normalize-input.outputs.request-artifact-id }}
          merge-multiple: true
          path: .three-ci-validation/request
      - uses: actions/download-artifact@v4
        if: ${{ needs.plan.outputs.plan-artifact-id != '' }}
        with:
          artifact-ids: ${{ needs.plan.outputs.plan-artifact-id }}
          merge-multiple: true
          path: .three-ci-validation/plan
      - name: Download execution-batch manifest
        if: ${{ needs.materialize-execution-batches.outputs.execution-batch-manifest-artifact-id != '' }}
        uses: actions/download-artifact@v4
        with:
          artifact-ids: ${{ needs.materialize-execution-batches.outputs.execution-batch-manifest-artifact-id }}
          merge-multiple: true
          path: .three-ci-validation/materialize
      - name: Download changed-files snapshot
        if: ${{ needs.plan.outputs.changed-files-snapshot-artifact-id != '' }}
        uses: actions/download-artifact@v4
        with:
          artifact-ids: ${{ needs.plan.outputs.changed-files-snapshot-artifact-id }}
          merge-multiple: true
          path: .three-ci-validation/plan
      - name: Download fact snapshot
        if: ${{ needs.plan.outputs.fact-snapshot-artifact-id != '' }}
        uses: actions/download-artifact@v4
        with:
          artifact-ids: ${{ needs.plan.outputs.fact-snapshot-artifact-id }}
          merge-multiple: true
          path: .three-ci-validation/plan
      - name: Download prerequisite batch evidence
        if: ${{ needs.materialize-execution-batches.outputs.has-execution-batch-layer-0 == 'true' }}
        env:
          GH_TOKEN: ${{ github.token }}
          CI_REPOSITORY: ${{ github.repository }}
          EXPECTED_DEPENDENCY_BUNDLES_JSON: ${{ toJson(matrix['expected-dependency-bundles']) }}
        shell: bash
        run: |
          mkdir -p .three-ci-validation/observed-artifacts
          while IFS=$'\t' read -r artifact_name artifact_path metadata_path; do
            if [ -z "$artifact_name" ]; then
              continue
            fi
                      rm -rf "$artifact_path"
                      mkdir -p "$artifact_path"
                      if ! artifact_json="$(gh api \
                        --repo "$CI_REPOSITORY" \
                        -H "Accept: application/vnd.github+json" \
                        "/repos/$CI_REPOSITORY/actions/runs/$GITHUB_RUN_ID/artifacts?name=$artifact_name")"; then
                        echo "::warning::Expected dependency artifact ${artifact_name} could not be listed"
                        rm -rf "$artifact_path"
                        continue
                      fi
                      if ! artifact_id="$(ARTIFACT_NAME="$artifact_name" ARTIFACT_JSON="$artifact_json" python - <<'PY'
          import json
          import os

          document = json.loads(os.environ["ARTIFACT_JSON"])
          matches = [
              artifact
              for artifact in document.get("artifacts", [])
              if artifact.get("name") == os.environ["ARTIFACT_NAME"]
              and artifact.get("expired") is not True
          ]
          if len(matches) != 1 or not matches[0].get("id"):
              raise SystemExit(
                  f"expected exactly one live dependency artifact named "
                  f"{os.environ['ARTIFACT_NAME']}, found {len(matches)}"
              )
          print(matches[0]["id"])
          PY
          )"; then
                        echo "::warning::Expected dependency artifact ${artifact_name} was not uniquely available"
                        rm -rf "$artifact_path"
                        continue
                      fi
                      archive_path="${artifact_path}.zip"
                      if ! gh api \
                        --repo "$CI_REPOSITORY" \
                        -H "Accept: application/zip" \
                        "/repos/$CI_REPOSITORY/actions/artifacts/${artifact_id}/zip" \
                        > "$archive_path"; then
                        echo "::warning::Expected dependency artifact ${artifact_name} (${artifact_id}) could not be downloaded"
                        rm -rf "$artifact_path" "$archive_path"
                        continue
                      fi
                      if ! unzip -q "$archive_path" -d "$artifact_path"; then
                        echo "::warning::Expected dependency artifact ${artifact_name} (${artifact_id}) could not be extracted"
                        rm -rf "$artifact_path" "$archive_path"
                        continue
                      fi
                      rm -f "$archive_path"
                      if [ ! -f "${artifact_path}/batch-evidence-bundle.json" ]; then
                        echo "::warning::Expected dependency artifact ${artifact_name} (${artifact_id}) did not contain batch-evidence-bundle.json"
                        rm -rf "$artifact_path"
                        continue
                      fi
                      if ! ARTIFACT_ID="$artifact_id" ARTIFACT_NAME="$artifact_name" ARTIFACT_PATH="$artifact_path" METADATA_PATH="$metadata_path" python - <<'PY'
          import json
          import os
          from pathlib import Path

          bundle_path = Path(os.environ["ARTIFACT_PATH"]) / "batch-evidence-bundle.json"
          bundle = json.loads(bundle_path.read_text(encoding="utf-8"))
          metadata = {
              "artifact-instance-id": os.environ["ARTIFACT_ID"],
              "artifact-ref": bundle["artifact-ref"],
              "physical-artifact-name": os.environ["ARTIFACT_NAME"],
              "run-id": os.environ["GITHUB_RUN_ID"],
              "run-attempt": os.environ["GITHUB_RUN_ATTEMPT"],
              "producer-boundary": "execution-batch",
          }
          Path(os.environ["METADATA_PATH"]).write_text(
              json.dumps(metadata, separators=(",", ":"), sort_keys=True),
              encoding="utf-8",
          )
          PY
                      then
                        echo "::warning::Expected dependency artifact ${artifact_name} (${artifact_id}) did not contain readable trusted bundle identity"
                        rm -rf "$artifact_path"
                        continue
                      fi
          done < <(
            python - <<'PY'
          import json
          import os

          for bundle in json.loads(os.environ["EXPECTED_DEPENDENCY_BUNDLES_JSON"] or "[]"):
              print(f"{bundle['artifact-name']}\t{bundle['artifact-path']}\t{bundle['artifact-metadata-path']}")
          PY
          )
      - name: Run batch validation commands
        id: validation
        shell: bash
        env:
          MATRIX_BATCH_JSON: ${{ toJson(matrix) }}
        run: |
          mkdir -p .three-ci-validation/work/validation-results
          shopt -s nullglob globstar
          dependency_bundle_args=()
          for bundle in .three-ci-validation/observed-artifacts/**/batch-evidence-bundle.json; do
            dependency_bundle_args+=(--dependency-bundle "$bundle")
          done
          observed_commit_sha="$(git rev-parse HEAD)"
          uv run python eng/scripts/workflow_release_control.py run-ci-validation-batch-commands \
            --plan .three-ci-validation/plan/validation-plan.json \
            --request .three-ci-validation/request/ci-validation-request.json \
            --execution-batch-manifest .three-ci-validation/materialize/execution-batch-manifest.json \
            --changed-files-snapshot .three-ci-validation/plan/changed-files.json \
            --fact-snapshot .three-ci-validation/plan/fact-snapshot.json \
            --expected-run-id "$GITHUB_RUN_ID" \
            --expected-run-attempt "$GITHUB_RUN_ATTEMPT" \
            --observed-artifacts-dir .three-ci-validation/observed-artifacts \
            --observed-commit-sha "$observed_commit_sha" \
            --matrix-row-json "$MATRIX_BATCH_JSON" \
            --repo-root . \
            --result-out-dir .three-ci-validation/work/validation-results \
            "${dependency_bundle_args[@]}" \
            --github-output "$GITHUB_OUTPUT"
      - name: Write batch evidence bundle
        id: bundle
        shell: bash
        env:
          MATRIX_BATCH_JSON: ${{ toJson(matrix) }}
        run: |
          shopt -s nullglob globstar
          validation_result_args=()
          for result in .three-ci-validation/work/validation-results/*.json; do
            validation_result_args+=(--validation-result "$result")
          done
          dependency_bundle_args=()
          for bundle in .three-ci-validation/observed-artifacts/**/batch-evidence-bundle.json; do
            dependency_bundle_args+=(--dependency-bundle "$bundle")
          done
          observed_commit_sha="$(git rev-parse HEAD)"
          uv run python eng/scripts/workflow_release_control.py write-ci-validation-batch-evidence-bundle \
            --plan .three-ci-validation/plan/validation-plan.json \
            --request .three-ci-validation/request/ci-validation-request.json \
            --execution-batch-manifest .three-ci-validation/materialize/execution-batch-manifest.json \
            --changed-files-snapshot .three-ci-validation/plan/changed-files.json \
            --fact-snapshot .three-ci-validation/plan/fact-snapshot.json \
            --matrix-row-json "$MATRIX_BATCH_JSON" \
            --expected-run-id "$GITHUB_RUN_ID" \
            --expected-run-attempt "$GITHUB_RUN_ATTEMPT" \
            --workflow "CI Validation" \
            --job "execution-batch" \
            --observed-artifacts-dir .three-ci-validation/observed-artifacts \
            --observed-commit-sha "$observed_commit_sha" \
            "${validation_result_args[@]}" \
            "${dependency_bundle_args[@]}" \
            --bundle-out .three-ci-validation/work/batch-evidence-bundle.json \
            --github-output "$GITHUB_OUTPUT"
      - name: Upload batch evidence bundle
        id: upload-batch-evidence
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.bundle.outputs.batch_evidence_bundle_artifact_name }}
          path: |
            .three-ci-validation/work/batch-evidence-bundle.json
            .three-ci-validation/work/validation-results/*.json
          include-hidden-files: true
          if-no-files-found: error
          retention-days: 30
  execution-batch-layer-2:
    name: execution-batch-layer-2
    needs:
      - normalize-input
      - plan
      - materialize-execution-batches
      - execution-batch-layer-0
      - execution-batch-layer-1
    if: always() && needs.materialize-execution-batches.outputs.has-execution-batch-layer-2 == 'true'
    runs-on: ${{ matrix['runner-family'] == 'windows' && 'windows-latest' || 'ubuntu-latest' }}
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.materialize-execution-batches.outputs.execution-batch-layer-2-matrix) }}
    permissions:
      contents: read
      actions: read
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: false
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
      - name: Install uv
        id: setup-uv
        continue-on-error: true
        uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0
        with:
          version: '0.10.9'
      - name: Retry uv installation
        if: ${{ steps.setup-uv.outcome == 'failure' }}
        uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0
        with:
          version: '0.10.9'
      - name: Install Node.js for JavaScript/TypeScript validation
        if: ${{ matrix.ecosystem == 'javascript' || matrix.ecosystem == 'typescript' }}
        uses: actions/setup-node@v4
        with:
          node-version: '24'
      - name: Enable pnpm for JavaScript/TypeScript validation
        if: ${{ matrix.ecosystem == 'javascript' || matrix.ecosystem == 'typescript' }}
        shell: bash
        run: corepack enable pnpm
      - name: Install Ruby for RubyGems validation
        if: ${{ matrix.ecosystem == 'ruby' }}
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.3'
      - uses: actions/download-artifact@v4
        if: ${{ needs.normalize-input.outputs.request-artifact-id != '' }}
        with:
          artifact-ids: ${{ needs.normalize-input.outputs.request-artifact-id }}
          merge-multiple: true
          path: .three-ci-validation/request
      - uses: actions/download-artifact@v4
        if: ${{ needs.plan.outputs.plan-artifact-id != '' }}
        with:
          artifact-ids: ${{ needs.plan.outputs.plan-artifact-id }}
          merge-multiple: true
          path: .three-ci-validation/plan
      - name: Download execution-batch manifest
        if: ${{ needs.materialize-execution-batches.outputs.execution-batch-manifest-artifact-id != '' }}
        uses: actions/download-artifact@v4
        with:
          artifact-ids: ${{ needs.materialize-execution-batches.outputs.execution-batch-manifest-artifact-id }}
          merge-multiple: true
          path: .three-ci-validation/materialize
      - name: Download changed-files snapshot
        if: ${{ needs.plan.outputs.changed-files-snapshot-artifact-id != '' }}
        uses: actions/download-artifact@v4
        with:
          artifact-ids: ${{ needs.plan.outputs.changed-files-snapshot-artifact-id }}
          merge-multiple: true
          path: .three-ci-validation/plan
      - name: Download fact snapshot
        if: ${{ needs.plan.outputs.fact-snapshot-artifact-id != '' }}
        uses: actions/download-artifact@v4
        with:
          artifact-ids: ${{ needs.plan.outputs.fact-snapshot-artifact-id }}
          merge-multiple: true
          path: .three-ci-validation/plan
      - name: Download prerequisite batch evidence
        if: ${{ needs.materialize-execution-batches.outputs.has-execution-batch-layer-1 == 'true' }}
        env:
          GH_TOKEN: ${{ github.token }}
          CI_REPOSITORY: ${{ github.repository }}
          EXPECTED_DEPENDENCY_BUNDLES_JSON: ${{ toJson(matrix['expected-dependency-bundles']) }}
        shell: bash
        run: |
          mkdir -p .three-ci-validation/observed-artifacts
          while IFS=$'\t' read -r artifact_name artifact_path metadata_path; do
            if [ -z "$artifact_name" ]; then
              continue
            fi
                      rm -rf "$artifact_path"
                      mkdir -p "$artifact_path"
                      if ! artifact_json="$(gh api \
                        --repo "$CI_REPOSITORY" \
                        -H "Accept: application/vnd.github+json" \
                        "/repos/$CI_REPOSITORY/actions/runs/$GITHUB_RUN_ID/artifacts?name=$artifact_name")"; then
                        echo "::warning::Expected dependency artifact ${artifact_name} could not be listed"
                        rm -rf "$artifact_path"
                        continue
                      fi
                      if ! artifact_id="$(ARTIFACT_NAME="$artifact_name" ARTIFACT_JSON="$artifact_json" python - <<'PY'
          import json
          import os

          document = json.loads(os.environ["ARTIFACT_JSON"])
          matches = [
              artifact
              for artifact in document.get("artifacts", [])
              if artifact.get("name") == os.environ["ARTIFACT_NAME"]
              and artifact.get("expired") is not True
          ]
          if len(matches) != 1 or not matches[0].get("id"):
              raise SystemExit(
                  f"expected exactly one live dependency artifact named "
                  f"{os.environ['ARTIFACT_NAME']}, found {len(matches)}"
              )
          print(matches[0]["id"])
          PY
          )"; then
                        echo "::warning::Expected dependency artifact ${artifact_name} was not uniquely available"
                        rm -rf "$artifact_path"
                        continue
                      fi
                      archive_path="${artifact_path}.zip"
                      if ! gh api \
                        --repo "$CI_REPOSITORY" \
                        -H "Accept: application/zip" \
                        "/repos/$CI_REPOSITORY/actions/artifacts/${artifact_id}/zip" \
                        > "$archive_path"; then
                        echo "::warning::Expected dependency artifact ${artifact_name} (${artifact_id}) could not be downloaded"
                        rm -rf "$artifact_path" "$archive_path"
                        continue
                      fi
                      if ! unzip -q "$archive_path" -d "$artifact_path"; then
                        echo "::warning::Expected dependency artifact ${artifact_name} (${artifact_id}) could not be extracted"
                        rm -rf "$artifact_path" "$archive_path"
                        continue
                      fi
                      rm -f "$archive_path"
                      if [ ! -f "${artifact_path}/batch-evidence-bundle.json" ]; then
                        echo "::warning::Expected dependency artifact ${artifact_name} (${artifact_id}) did not contain batch-evidence-bundle.json"
                        rm -rf "$artifact_path"
                        continue
                      fi
                      if ! ARTIFACT_ID="$artifact_id" ARTIFACT_NAME="$artifact_name" ARTIFACT_PATH="$artifact_path" METADATA_PATH="$metadata_path" python - <<'PY'
          import json
          import os
          from pathlib import Path

          bundle_path = Path(os.environ["ARTIFACT_PATH"]) / "batch-evidence-bundle.json"
          bundle = json.loads(bundle_path.read_text(encoding="utf-8"))
          metadata = {
              "artifact-instance-id": os.environ["ARTIFACT_ID"],
              "artifact-ref": bundle["artifact-ref"],
              "physical-artifact-name": os.environ["ARTIFACT_NAME"],
              "run-id": os.environ["GITHUB_RUN_ID"],
              "run-attempt": os.environ["GITHUB_RUN_ATTEMPT"],
              "producer-boundary": "execution-batch",
          }
          Path(os.environ["METADATA_PATH"]).write_text(
              json.dumps(metadata, separators=(",", ":"), sort_keys=True),
              encoding="utf-8",
          )
          PY
                      then
                        echo "::warning::Expected dependency artifact ${artifact_name} (${artifact_id}) did not contain readable trusted bundle identity"
                        rm -rf "$artifact_path"
                        continue
                      fi
          done < <(
            python - <<'PY'
          import json
          import os

          for bundle in json.loads(os.environ["EXPECTED_DEPENDENCY_BUNDLES_JSON"] or "[]"):
              print(f"{bundle['artifact-name']}\t{bundle['artifact-path']}\t{bundle['artifact-metadata-path']}")
          PY
          )
      - name: Run batch validation commands
        id: validation
        shell: bash
        env:
          MATRIX_BATCH_JSON: ${{ toJson(matrix) }}
        run: |
          mkdir -p .three-ci-validation/work/validation-results
          shopt -s nullglob globstar
          dependency_bundle_args=()
          for bundle in .three-ci-validation/observed-artifacts/**/batch-evidence-bundle.json; do
            dependency_bundle_args+=(--dependency-bundle "$bundle")
          done
          observed_commit_sha="$(git rev-parse HEAD)"
          uv run python eng/scripts/workflow_release_control.py run-ci-validation-batch-commands \
            --plan .three-ci-validation/plan/validation-plan.json \
            --request .three-ci-validation/request/ci-validation-request.json \
            --execution-batch-manifest .three-ci-validation/materialize/execution-batch-manifest.json \
            --changed-files-snapshot .three-ci-validation/plan/changed-files.json \
            --fact-snapshot .three-ci-validation/plan/fact-snapshot.json \
            --expected-run-id "$GITHUB_RUN_ID" \
            --expected-run-attempt "$GITHUB_RUN_ATTEMPT" \
            --observed-artifacts-dir .three-ci-validation/observed-artifacts \
            --observed-commit-sha "$observed_commit_sha" \
            --matrix-row-json "$MATRIX_BATCH_JSON" \
            --repo-root . \
            --result-out-dir .three-ci-validation/work/validation-results \
            "${dependency_bundle_args[@]}" \
            --github-output "$GITHUB_OUTPUT"
      - name: Write batch evidence bundle
        id: bundle
        shell: bash
        env:
          MATRIX_BATCH_JSON: ${{ toJson(matrix) }}
        run: |
          shopt -s nullglob globstar
          validation_result_args=()
          for result in .three-ci-validation/work/validation-results/*.json; do
            validation_result_args+=(--validation-result "$result")
          done
          dependency_bundle_args=()
          for bundle in .three-ci-validation/observed-artifacts/**/batch-evidence-bundle.json; do
            dependency_bundle_args+=(--dependency-bundle "$bundle")
          done
          observed_commit_sha="$(git rev-parse HEAD)"
          uv run python eng/scripts/workflow_release_control.py write-ci-validation-batch-evidence-bundle \
            --plan .three-ci-validation/plan/validation-plan.json \
            --request .three-ci-validation/request/ci-validation-request.json \
            --execution-batch-manifest .three-ci-validation/materialize/execution-batch-manifest.json \
            --changed-files-snapshot .three-ci-validation/plan/changed-files.json \
            --fact-snapshot .three-ci-validation/plan/fact-snapshot.json \
            --matrix-row-json "$MATRIX_BATCH_JSON" \
            --expected-run-id "$GITHUB_RUN_ID" \
            --expected-run-attempt "$GITHUB_RUN_ATTEMPT" \
            --workflow "CI Validation" \
            --job "execution-batch" \
            --observed-artifacts-dir .three-ci-validation/observed-artifacts \
            --observed-commit-sha "$observed_commit_sha" \
            "${validation_result_args[@]}" \
            "${dependency_bundle_args[@]}" \
            --bundle-out .three-ci-validation/work/batch-evidence-bundle.json \
            --github-output "$GITHUB_OUTPUT"
      - name: Upload batch evidence bundle
        id: upload-batch-evidence
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.bundle.outputs.batch_evidence_bundle_artifact_name }}
          path: |
            .three-ci-validation/work/batch-evidence-bundle.json
            .three-ci-validation/work/validation-results/*.json
          include-hidden-files: true
          if-no-files-found: error
          retention-days: 30

  aggregate-evidence:
    name: aggregate-evidence
    needs:
      - normalize-input
      - plan
      - materialize-execution-batches
      - execution-batch-layer-0
      - execution-batch-layer-1
      - execution-batch-layer-2
    if: always()
    runs-on: ubuntu-latest
    permissions:
      contents: read
      actions: read
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
          persist-credentials: false
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
      - name: Install uv
        id: setup-uv
        continue-on-error: true
        uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0
        with:
          version: '0.10.9'
      - name: Retry uv installation
        if: ${{ steps.setup-uv.outcome == 'failure' }}
        uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0
        with:
          version: '0.10.9'
      - name: Prepare aggregate input directories
        run: |
          mkdir -p \
            .three-ci-validation/request \
            .three-ci-validation/plan \
            .three-ci-validation/materialize \
            .three-ci-validation/observed-artifacts \
            .three-ci-validation/aggregate
      - name: Download CI validation request
        if: ${{ needs.normalize-input.outputs.request-artifact-id != '' }}
        uses: actions/download-artifact@v4
        with:
          artifact-ids: ${{ needs.normalize-input.outputs.request-artifact-id }}
          merge-multiple: true
          path: .three-ci-validation/request
      - name: Download validation plan
        if: ${{ needs.plan.outputs.plan-artifact-id != '' }}
        uses: actions/download-artifact@v4
        with:
          artifact-ids: ${{ needs.plan.outputs.plan-artifact-id }}
          merge-multiple: true
          path: .three-ci-validation/plan
      - name: Download execution-batch manifest
        if: ${{ needs.materialize-execution-batches.outputs.execution-batch-manifest-artifact-id != '' }}
        uses: actions/download-artifact@v4
        with:
          artifact-ids: ${{ needs.materialize-execution-batches.outputs.execution-batch-manifest-artifact-id }}
          merge-multiple: true
          path: .three-ci-validation/materialize
      - name: Download changed-files snapshot
        if: ${{ needs.plan.outputs.changed-files-snapshot-artifact-id != '' }}
        uses: actions/download-artifact@v4
        with:
          artifact-ids: ${{ needs.plan.outputs.changed-files-snapshot-artifact-id }}
          merge-multiple: true
          path: .three-ci-validation/plan
      - name: Download fact snapshot
        if: ${{ needs.plan.outputs.fact-snapshot-artifact-id != '' }}
        uses: actions/download-artifact@v4
        with:
          artifact-ids: ${{ needs.plan.outputs.fact-snapshot-artifact-id }}
          merge-multiple: true
          path: .three-ci-validation/plan
      - name: Download observed batch evidence
        env:
          GH_TOKEN: ${{ github.token }}
          CI_REPOSITORY: ${{ github.repository }}
        run: |
          mkdir -p .three-ci-validation/observed-artifacts .three-ci-validation/aggregate
          aggregate_started_at="$(date -u +%Y-%m-%dT%H:%M:%SZ)"
          printf '%s\n' "$aggregate_started_at" > .three-ci-validation/aggregate/aggregate-started-at.txt
          uv run python eng/scripts/workflow_release_control.py download-ci-validation-observed-artifacts \
            --repository "$CI_REPOSITORY" \
            --run-id "$GITHUB_RUN_ID" \
            --run-attempt "$GITHUB_RUN_ATTEMPT" \
            --plan .three-ci-validation/plan/validation-plan.json \
            --execution-batch-manifest .three-ci-validation/materialize/execution-batch-manifest.json \
            --observed-artifacts-dir .three-ci-validation/observed-artifacts
      - name: Write aggregate evidence manifest
        env:
          GH_TOKEN: ${{ github.token }}
          CI_REPOSITORY: ${{ github.repository }}
          REQUEST_ARTIFACT_ID: ${{ needs.normalize-input.outputs.request-artifact-id }}
          PLAN_ARTIFACT_ID: ${{ needs.plan.outputs.plan-artifact-id }}
          CHANGED_FILES_ARTIFACT_ID: ${{ needs.plan.outputs.changed-files-snapshot-artifact-id }}
          FACT_SNAPSHOT_ARTIFACT_ID: ${{ needs.plan.outputs.fact-snapshot-artifact-id }}
          EXECUTION_BATCH_MANIFEST_ARTIFACT_ID: ${{ needs.materialize-execution-batches.outputs.execution-batch-manifest-artifact-id }}
        run: |
          mkdir -p .three-ci-validation/aggregate
          aggregate_started_at="$(cat .three-ci-validation/aggregate/aggregate-started-at.txt)"
          aggregate_args=(
            --repository "$CI_REPOSITORY"
            --workflow "CI Validation"
            --run-id "$GITHUB_RUN_ID"
            --run-attempt "$GITHUB_RUN_ATTEMPT"
            --request .three-ci-validation/request/ci-validation-request.json
            --plan .three-ci-validation/plan/validation-plan.json
            --changed-files-snapshot .three-ci-validation/plan/changed-files.json
            --fact-snapshot .three-ci-validation/plan/fact-snapshot.json
            --observed-artifacts-dir .three-ci-validation/observed-artifacts
            --expected-request-artifact-id "$REQUEST_ARTIFACT_ID"
            --expected-plan-artifact-id "$PLAN_ARTIFACT_ID"
            --created-at "$aggregate_started_at"
            --aggregate-phase evidence
            --aggregate-evidence-manifest-out .three-ci-validation/aggregate/aggregate-evidence-manifest.json
            --aggregate-summary-out .three-ci-validation/aggregate/aggregate-summary.json
          )
          if [ -n "$EXECUTION_BATCH_MANIFEST_ARTIFACT_ID" ]; then
            aggregate_args+=(
              --execution-batch-manifest .three-ci-validation/materialize/execution-batch-manifest.json
              --expected-execution-batch-manifest-artifact-id "$EXECUTION_BATCH_MANIFEST_ARTIFACT_ID"
            )
          else
            aggregate_args+=(--batch-materialization-failed)
          fi
          if [ -n "$CHANGED_FILES_ARTIFACT_ID" ]; then
            aggregate_args+=(--expected-changed-files-snapshot-artifact-id "$CHANGED_FILES_ARTIFACT_ID")
          fi
          if [ -n "$FACT_SNAPSHOT_ARTIFACT_ID" ]; then
            aggregate_args+=(--expected-fact-snapshot-artifact-id "$FACT_SNAPSHOT_ARTIFACT_ID")
          fi
          uv run python eng/scripts/workflow_release_control.py aggregate-ci-evidence "${aggregate_args[@]}"
      - name: Upload aggregate evidence manifest
        if: always()
        id: upload-aggregate-evidence-manifest
        uses: actions/upload-artifact@v4
        with:
          name: ${{ needs.normalize-input.outputs.aggregate-evidence-manifest-artifact-name }}
          path: .three-ci-validation/aggregate/aggregate-evidence-manifest.json
          include-hidden-files: true
          if-no-files-found: ignore
          retention-days: 30
      - name: Verify aggregate evidence manifest producer boundary
        if: always() && steps.upload-aggregate-evidence-manifest.outputs.artifact-id != ''
        id: verify-aggregate-evidence-manifest-producer-boundary
        env:
          GH_TOKEN: ${{ github.token }}
          AGGREGATE_EVIDENCE_MANIFEST_REF: ci-validation/aggregate/${{ github.run_id }}/${{ github.run_attempt }}/aggregate-evidence-manifest.json
          AGGREGATE_EVIDENCE_MANIFEST_ARTIFACT_ID: ${{ steps.upload-aggregate-evidence-manifest.outputs.artifact-id }}
        run: |
          uv run python eng/scripts/workflow_release_control.py verify-ci-validation-artifact-boundaries \
            --repository "$GITHUB_REPOSITORY" \
            --workflow "CI Validation" \
            --run-id "$GITHUB_RUN_ID" \
            --run-attempt "$GITHUB_RUN_ATTEMPT" \
            --max-prefixed-validation-artifacts 18 \
            --expected-artifact "{\"artifact-ref\":\"${AGGREGATE_EVIDENCE_MANIFEST_REF}\",\"artifact-instance-id\":\"${AGGREGATE_EVIDENCE_MANIFEST_ARTIFACT_ID}\",\"producer-boundary\":\"aggregate-evidence\",\"producer-job\":\"aggregate-evidence\"}"
      - name: Write aggregate summary
        if: always()
        env:
          GH_TOKEN: ${{ github.token }}
          CI_REPOSITORY: ${{ github.repository }}
          REQUEST_ARTIFACT_ID: ${{ needs.normalize-input.outputs.request-artifact-id }}
          PLAN_ARTIFACT_ID: ${{ needs.plan.outputs.plan-artifact-id }}
          CHANGED_FILES_ARTIFACT_ID: ${{ needs.plan.outputs.changed-files-snapshot-artifact-id }}
          FACT_SNAPSHOT_ARTIFACT_ID: ${{ needs.plan.outputs.fact-snapshot-artifact-id }}
          EXECUTION_BATCH_MANIFEST_ARTIFACT_ID: ${{ needs.materialize-execution-batches.outputs.execution-batch-manifest-artifact-id }}
          AGGREGATE_EVIDENCE_MANIFEST_ARTIFACT_ID: ${{ steps.upload-aggregate-evidence-manifest.outputs.artifact-id }}
          AGGREGATE_EVIDENCE_MANIFEST_PRODUCER_VERIFIED: ${{ steps.verify-aggregate-evidence-manifest-producer-boundary.outcome == 'success' }}
        run: |
          mkdir -p .three-ci-validation/aggregate
          aggregate_completed_at="$(date -u +%Y-%m-%dT%H:%M:%SZ)"
          if [ -s .three-ci-validation/aggregate/aggregate-started-at.txt ]; then
            aggregate_started_at="$(cat .three-ci-validation/aggregate/aggregate-started-at.txt)"
          else
            aggregate_started_at="$aggregate_completed_at"
          fi
          aggregate_args=(
            --repository "$CI_REPOSITORY"
            --workflow "CI Validation"
            --run-id "$GITHUB_RUN_ID"
            --run-attempt "$GITHUB_RUN_ATTEMPT"
            --request .three-ci-validation/request/ci-validation-request.json
            --plan .three-ci-validation/plan/validation-plan.json
            --changed-files-snapshot .three-ci-validation/plan/changed-files.json
            --fact-snapshot .three-ci-validation/plan/fact-snapshot.json
            --observed-artifacts-dir .three-ci-validation/observed-artifacts
            --expected-request-artifact-id "$REQUEST_ARTIFACT_ID"
            --expected-plan-artifact-id "$PLAN_ARTIFACT_ID"
            --aggregate-evidence-manifest-artifact-id "$AGGREGATE_EVIDENCE_MANIFEST_ARTIFACT_ID"
            --started-at "$aggregate_started_at"
            --completed-at "$aggregate_completed_at"
            --created-at "$aggregate_completed_at"
            --aggregate-phase summary
            --aggregate-evidence-manifest-out .three-ci-validation/aggregate/aggregate-evidence-manifest.json
            --aggregate-summary-out .three-ci-validation/aggregate/aggregate-summary.json
          )
          if [ -n "$EXECUTION_BATCH_MANIFEST_ARTIFACT_ID" ]; then
            aggregate_args+=(
              --execution-batch-manifest .three-ci-validation/materialize/execution-batch-manifest.json
              --expected-execution-batch-manifest-artifact-id "$EXECUTION_BATCH_MANIFEST_ARTIFACT_ID"
            )
          else
            aggregate_args+=(--batch-materialization-failed)
          fi
          if [ -n "$CHANGED_FILES_ARTIFACT_ID" ]; then
            aggregate_args+=(--expected-changed-files-snapshot-artifact-id "$CHANGED_FILES_ARTIFACT_ID")
          fi
          if [ -n "$FACT_SNAPSHOT_ARTIFACT_ID" ]; then
            aggregate_args+=(--expected-fact-snapshot-artifact-id "$FACT_SNAPSHOT_ARTIFACT_ID")
          fi
          if [ "$AGGREGATE_EVIDENCE_MANIFEST_PRODUCER_VERIFIED" = "true" ]; then
            aggregate_args+=(--aggregate-evidence-manifest-producer-verified)
          fi
          uv run python eng/scripts/workflow_release_control.py aggregate-ci-evidence "${aggregate_args[@]}"
      - name: Upload aggregate summary
        if: always()
        id: upload-aggregate-summary
        uses: actions/upload-artifact@v4
        with:
          name: ${{ needs.normalize-input.outputs.aggregate-summary-artifact-name }}
          path: .three-ci-validation/aggregate/aggregate-summary.json
          include-hidden-files: true
          if-no-files-found: ignore
          retention-days: 30
      - name: Download uploaded aggregate evidence manifest
        if: always() && steps.upload-aggregate-evidence-manifest.outputs.artifact-id != ''
        uses: actions/download-artifact@v4
        with:
          artifact-ids: ${{ steps.upload-aggregate-evidence-manifest.outputs.artifact-id }}
          merge-multiple: true
          path: .three-ci-validation/final-uploaded/aggregate-evidence-manifest
      - name: Download uploaded aggregate summary
        if: always() && steps.upload-aggregate-summary.outputs.artifact-id != ''
        uses: actions/download-artifact@v4
        with:
          artifact-ids: ${{ steps.upload-aggregate-summary.outputs.artifact-id }}
          merge-multiple: true
          path: .three-ci-validation/final-uploaded/aggregate-summary
      - name: Verify final artifact bytes and producer boundaries
        if: always()
        env:
          GH_TOKEN: ${{ github.token }}
          AGGREGATE_EVIDENCE_MANIFEST_REF: ci-validation/aggregate/${{ github.run_id }}/${{ github.run_attempt }}/aggregate-evidence-manifest.json
          AGGREGATE_SUMMARY_REF: ci-validation/aggregate/${{ github.run_id }}/${{ github.run_attempt }}/aggregate-summary.json
          AGGREGATE_EVIDENCE_MANIFEST_ARTIFACT_ID: ${{ steps.upload-aggregate-evidence-manifest.outputs.artifact-id }}
          AGGREGATE_SUMMARY_ARTIFACT_ID: ${{ steps.upload-aggregate-summary.outputs.artifact-id }}
        run: |
          read -r expected_prefixed_validation_artifacts aggregate_manifest_digest aggregate_summary_digest <<EOF
          $(uv run python - <<'PY'
          import hashlib
          import json
          from pathlib import Path

          summary = json.loads(Path('.three-ci-validation/aggregate/aggregate-summary.json').read_text(encoding='utf-8'))
          aggregate_manifest = summary.get('aggregate-evidence-manifest', {})
          aggregate_manifest_digest = aggregate_manifest.get('content-digest') or ''
          aggregate_summary_digest = hashlib.sha256(Path('.three-ci-validation/aggregate/aggregate-summary.json').read_bytes()).hexdigest()
          print(summary['budgets']['expected-actual-validation-artifacts'], aggregate_manifest_digest, aggregate_summary_digest)
          PY
          )
          EOF
          expected_artifacts=()
          expected_artifacts+=(--expected-artifact "{\"artifact-ref\":\"${AGGREGATE_EVIDENCE_MANIFEST_REF}\",\"artifact-instance-id\":\"${AGGREGATE_EVIDENCE_MANIFEST_ARTIFACT_ID}\",\"producer-boundary\":\"aggregate-evidence\",\"producer-job\":\"aggregate-evidence\",\"downloaded-path\":\".three-ci-validation/final-uploaded/aggregate-evidence-manifest/aggregate-evidence-manifest.json\",\"content-digest\":\"${aggregate_manifest_digest}\"}")
          expected_artifacts+=(--expected-artifact "{\"artifact-ref\":\"${AGGREGATE_SUMMARY_REF}\",\"artifact-instance-id\":\"${AGGREGATE_SUMMARY_ARTIFACT_ID}\",\"producer-boundary\":\"aggregate-evidence\",\"producer-job\":\"aggregate-evidence\",\"downloaded-path\":\".three-ci-validation/final-uploaded/aggregate-summary/aggregate-summary.json\",\"content-digest\":\"${aggregate_summary_digest}\"}")
          uv run python eng/scripts/workflow_release_control.py verify-ci-validation-artifact-boundaries \
           --repository "$GITHUB_REPOSITORY" \
           --workflow "CI Validation" \
           --run-id "$GITHUB_RUN_ID" \
           --run-attempt "$GITHUB_RUN_ATTEMPT" \
           --max-prefixed-validation-artifacts 20 \
           --expected-prefixed-validation-artifacts "$expected_prefixed_validation_artifacts" \
           "${expected_artifacts[@]}"
