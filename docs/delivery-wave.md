# Current Delivery Wave

This record is the sole positive work-authorization authority under the
[repository record policy](governance/record-system.md#work-authorization-and-concurrency).
Only its accepted `main` version grants work. Issues, PRs, sessions and proposed
edits cannot add to or enlarge a grant. Merge changes authorization; deletion
ends a grant. Git and the proposing PR retain the reason and history.

## Authorized Advancements

### Consolidate Diagnostic Inspection Test Owners

- **Work carrier:** [Issue #749](https://github.com/hcoona/three/issues/749) and its delivery and review carriers.
- **Authorized outcome:** Implement only `gc-review.json:GC-R02`, `GC-R03`
  and `GC-R04` from the independently reviewed
  [#738 analysis](https://github.com/hcoona/three/issues/738). Consolidate repeated
  fixed-response inspection projections into existing scenario owners in
  `src/public/lib/three-workflow-delivery-v3/tests/governance/test_commit10_inspection.py`.
  Bind the exact removal, affected-case and surviving-owner identities from the
  work carrier. The selection identifies five function removals, six assertion
  transfers into two existing owners and 26 surviving module cases. Its 18-case
  reduction is conditional, not a quota or delivered gain.
- **Prerequisites and governing inputs:** Require accepted #746 implementation
  and reconcile its final accepted source, tree, authorities and retained
  collection before dependent work. Follow the current
  [v3 handoff](../src/public/lib/three-workflow-delivery-v3/docs/agent-handoff.md),
  [requirements](../src/public/lib/three-workflow-delivery-v3/docs/requirements.md),
  relevant design and validation authorities and
  [shared testing principles](engineering/engineering-principles.md#testing).
  Retain the reviewed #738 reports, exact-byte recovery, finite selection,
  independent dispositions and residuals. Historical findings keep their
  original scope; withdrawn GC-N02 remains excluded. Refresh dependent evidence
  when source or relied-on authorities change.
- **Validation and review:** Transfer all six assertions atomically before
  deleting their former owners. The present-reviewer owner must retain exact
  `scope == "single-acceptance-review-recovery"` and the complete no-`mutation`
  assertion over every part of every recorded command. The exhausted-connection
  owner must retain the original nine-key allowed subset, including optional
  `schema`, and the three lowercase serialized-output exclusions for
  `capability`, `live_enabled` and `authorization`. Preserve subset semantics;
  do not require `schema` or claim unknown-input rejection. Reuse the two owners'
  existing document and runner values. Preserve their existing assertions, all
  five primary owner cases and all 26 module survivors, including present,
  removed and unknown outcomes, exact reviewer/environment/review identity,
  review-scoped nested cursors, later approved matches and shared deadlines.
  Preserve separate transport OSError and subprocess timeout, complete versus
  truncated package 404, terminal version-page absence and exhausted pagination.
  Reviewer inspection and historical acceptance transport remain independent
  boundaries; diagnostic recovery supplies no normal-Live or publication
  authority. Add no helper, fixture, parameter row or replacement command call.
  Reconcile exact pre/post nodes and surviving owners, with renames separate.
  Run the complete affected module and required existing full v3 and repository
  checks. Independently review implementation and domain/evidence boundaries;
  independently dispose material findings. Failed ownership assumptions retain
  the original test and require refreshed analysis. Report grouped-diagnostic
  limitations and distinguish controlled responses from actual platform
  capability or permission evidence. Follow the
  [agent-execution guidance](engineering/agent-execution.md).
- **Scope and effects:** Permit only the selected test module and specified
  assertion transfers, ordinary existing validation and dependency/tool
  operations, task-owned evidence and normal Git/Issue/PR delivery. Preserve
  #721's temporary-resource ownership, bounded lifecycle and failed/interrupted
  evidence. Existing isolated loopback and test-owned process lifetimes remain
  ordinary validation. Stop on resource exhaustion or an unreaped task session;
  retain diagnostics and resolve ownership before further execution. Production,
  actual workflows/HK configuration, dependencies, resource limits and
  historical/native evidence stay unchanged. Other #738 candidates, other v3
  development, native service probes, dispatch/rerun, external publication,
  administrative or authentication/access changes, host-wide cleanup and
  termination outside existing test-owned lifecycles are excluded. Existing
  npm/NuGet gates and spent-operation boundaries remain in force.
- **Completion boundary:** Retain actual node deltas, validation and cleanup
  observations, independent reviews and disposed findings in the work carrier.
  Do not claim runtime or peak-inode improvements without measurements. End or
  replace this finite grant through a reviewed Wave change after implementation
  and evidence are accepted. Other analysis proposals require their own
  accepted finite authorization.
