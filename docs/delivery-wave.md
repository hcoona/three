# Current Delivery Wave

This record is the sole positive work-authorization authority under the
[repository record policy](governance/record-system.md#work-authorization-and-concurrency).
Only its accepted `main` version grants work. Issues, PRs, sessions and proposed
edits cannot add to or enlarge a grant. Merge changes authorization; deletion
ends a grant. Git and the proposing PR retain the reason and history.

## Authorized Advancements

### Consolidate Workflow Contract Test Owners

- **Work carrier:** [Issue #746](https://github.com/hcoona/three/issues/746) and its delivery and review carriers.
- **Authorized outcome:** Implement the finite workflow-contract selection from
  the independently reviewed [#738 analysis](https://github.com/hcoona/three/issues/738):
  `coexistence-review.json:LC-01/02`,
  `buddy-workflows-review.json:BW-01/02/03` and
  `ci-nuget-workflows-review.json:CW-01`. Consolidate repeated retirement and
  finalizer source checks into existing complete owners. Bind exact removal,
  affected-case and surviving-owner identities from the work carrier.
  Conditional counts are not quotas or delivered gains.
- **Prerequisites and governing inputs:** Follow the current
  [v3 handoff](../src/public/lib/three-workflow-delivery-v3/docs/agent-handoff.md),
  [requirements](../src/public/lib/three-workflow-delivery-v3/docs/requirements.md),
  relevant design and validation authorities and
  [shared testing principles](engineering/engineering-principles.md#testing).
  Reconcile source and accepted authorities after the accepted #743 implementation.
  Retain the reviewed #738 reports, exact-byte recovery, finite selection,
  independent dispositions and residuals. BW-F01's two strict `overwrite is False`
  transfers and explicit action-pin owner are mandatory. Refresh dependent
  evidence when source or relied-on authorities change.
- **Validation and review:** Transfer every unique BW-02 and CW-01 assertion
  atomically before deleting its former owner. Preserve complete upload-name
  equality, strict Boolean checks, full upload dictionaries, exact conditions,
  unique step bindings, GitHub summary option, raw transport and action pins.
  Preserve the missing-Decision guard's exact terminal position and full literal
  condition. Keep both YAML suffixes, exact legacy predicates, topology detector
  and synthetic routes, direct job dependencies, target-binding guards, all
  existing shell scenarios and compatibility fences. Add no replacement cases,
  fixtures, workflow parses, process calls or loop inputs. Retain independent
  failure and caller boundaries and report grouped-diagnostic limitations.
  Reconcile exact pre/post nodes and all surviving owners, with renames separate.
  Run affected modules and retained integration boundaries together, then
  required existing full v3 and repository checks. Independently review
  implementation and domain/evidence boundaries; independently dispose material
  findings. Failed ownership assumptions retain the original test and require
  refreshed analysis. Follow the [agent-execution guidance](engineering/agent-execution.md).
- **Scope and effects:** Permit only the three selected test modules and their
  specified assertion transfers, ordinary existing validation and dependency/tool
  operations, task-owned evidence and normal Git/Issue/PR delivery. Preserve
  #721's temporary-resource ownership, bounded lifecycle and failed/interrupted
  evidence. Existing isolated loopback and test-owned process lifetimes remain
  ordinary validation. Stop on resource exhaustion or an unreaped task session;
  retain diagnostics and resolve ownership before further execution. Production,
  actual workflows/HK configuration, resource limits and historical/native
  evidence stay unchanged. Other #738 candidates, other v3 development, native
  service probes, dispatch/rerun, external publication, administrative or
  authentication/access changes, host-wide cleanup and termination outside
  existing test-owned lifecycles are excluded. Existing npm/NuGet gates and
  spent-operation boundaries remain in force.
- **Completion boundary:** Retain actual node deltas, validation and cleanup
  observations, independent reviews and disposed findings in the work carrier.
  End or replace this finite grant through a reviewed Wave change after
  implementation and evidence are accepted. Other analysis proposals require
  their own accepted finite authorization.
