# Current Delivery Wave

This record is the sole positive work-authorization authority under the
[repository record policy](governance/record-system.md#work-authorization-and-concurrency).
Only its accepted `main` version grants work. Issues, PRs, sessions and proposed
edits cannot add to or enlarge a grant. Merge changes authorization; deletion
ends a grant. Git and the proposing PR retain the reason and history.

## Authorized Advancements

### Consolidate Static-Reference Source and Live Admission Test Owners

- **Work carrier:** [Issue #752](https://github.com/hcoona/three/issues/752) and its delivery and review carriers.
- **Authorized outcome:** Implement only
  `static-foundation-review.json:SR-01–07`,
  `static-sidecar-review.json:SS-01–03` and
  `eligibility-review.json:EL-01` from the independently reviewed
  [#738 analysis](https://github.com/hcoona/three/issues/738). Consolidate repeated
  source, official-reader and Live admission scenarios into existing owners.
  Bind exact removal, affected-case and surviving-owner identities from the
  work carrier. The selection conditionally removes 24 cases across four
  modules while preserving 45 explicit owners and all 535 module survivors.
  Counts are not quotas or delivered gains.
- **Prerequisites and governing inputs:** Require accepted #749 implementation
  and separately reconcile its final accepted source, tree, authorities and
  retained collection before dependent work. Proposed Wave or implementation
  candidates do not satisfy acceptance. Follow the current
  [v3 handoff](../src/public/lib/three-workflow-delivery-v3/docs/agent-handoff.md),
  [requirements](../src/public/lib/three-workflow-delivery-v3/docs/requirements.md),
  relevant design and validation authorities and
  [shared testing principles](engineering/engineering-principles.md#testing).
  Retain the frozen #738 reports, exact-byte recovery, finite selection,
  independent dispositions and residuals. Preserve historical preparation
  receipts and carry accepted-source reconciliation separately. Refresh
  dependent evidence when source or relied-on authorities change.
- **Validation and review:** Transfer unique assertions into the five existing
  owner functions before deleting their former owners. Preserve complete clean
  serialization and scan/validation/Governance ordering; add literal result,
  error-kind and findings expectations to all eight existing Eligibility rows.
  Retain exact diagnostics, complete evidence, enabled Governance, exact scan
  and fresh-read assertions, and separate validation-rejection short circuit.
  Preserve the seeded source error's exact diagnostic, path and sanitized
  message, and full path/content-list cardinality for all three existing
  target/index/worktree inventories. Keep seven non-source terminal rows and
  exact cleanup. Retain precise target/policy/source admission with
  self-consistent outer digests, exact-tag success and replay boundaries.
  Preserve richer selected-input/HOME, dependency-section, source-base and
  alias owners. Transfer original bytes/digest and snapshot-root
  `node_modules` absence to the existing zero-BOM v9 call; the checks may cover
  all three existing BOM iterations without changing their inputs or the six
  existing authority calls. Preserve both CRLF ambient rows and loaded guards.
  Retain all 45 explicit owners and all 535 module survivors with their node
  identities. Keep source acquisition, materialization, official reader
  process/transport, normalized projection, policy cleanup and Live admission
  as separate boundaries. Add no helper, fixture, case, acquisition,
  evaluator/native-reader invocation or replay loop. Retire only the selected
  rows/functions and specifically identified newly unused imports/constants
  or unreachable setup; preserve remaining assertions and parameter IDs.
  Reconcile exact pre/post nodes and assertion ownership, with renames separate.
  Run complete affected/shared-owner and required existing full v3/repository
  checks. Independently review implementation and domain/evidence boundaries;
  independently dispose material findings. Failed ownership assumptions retain
  the original test and require refreshed analysis. Follow the
  [agent-execution guidance](engineering/agent-execution.md).
- **Evidence limits:** Preserve every selected residual. The focused clean Live
  helper substitutes validation/observation; per-error ordering replay retires.
  Both cited operational CLI failure rows exercise worktree. SS-02 relinquishes
  successful two-parent traversal. SS-03's retained LF input is nested and uses
  the existing default context; its absence check observes
  `snapshot_root/node_modules`, not the nested reader directory. Keep separate
  CRLF ambient coverage and guards without claiming identical side-effect
  coverage. Version/coercion requirements do not mandate literally eleven
  NuGet rows. Report grouped-diagnostic limitations; controlled results do not
  establish native capability or exhaustive equivalence.
- **Scope and effects:** Permit only `test_static_reference.py`,
  `test_live_static_reference.py`, `test_static_reference_sidecars.py` and
  `test_eligibility.py` under
  `src/public/lib/three-workflow-delivery-v3/tests/release/`, with the specified
  transfers and support cleanup, ordinary existing validation and
  dependency/tool operations, task-owned evidence and normal Git/Issue/PR
  delivery. Preserve #721's temporary-resource ownership, bounded lifecycle
  and failed/interrupted evidence. Existing isolated loopback and test-owned
  process lifetimes remain ordinary validation. Stop on resource exhaustion
  or an unreaped task session; retain diagnostics and resolve ownership before
  further execution. Production, workflows/HK configuration, dependency
  declarations, resource limits and historical/native evidence stay unchanged.
  Other candidates and withdrawn suggestions, other v3 development, native
  service probes, dispatch/rerun, external publication, administrative or
  authentication/access changes, host-wide cleanup and termination outside
  existing test-owned lifecycles are excluded. Existing npm/NuGet gates and
  spent-operation boundaries remain in force.
- **Completion boundary:** Retain actual node deltas, validation and cleanup
  observations, independent reviews and disposed findings in the work carrier.
  Claim runtime or peak-inode improvements only when measured. End or replace
  this finite grant through a reviewed Wave change after implementation and
  evidence are accepted. Other analysis proposals require their own accepted
  finite authorization.
