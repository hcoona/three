# Current Delivery Wave

This record is the sole positive work-authorization authority under the
[repository record policy](governance/record-system.md#work-authorization-and-concurrency).
Only its accepted `main` version grants work. Issues, PRs, sessions and proposed
edits cannot add to or enlarge a grant. Merge changes authorization; deletion
ends a grant. Git and the proposing PR retain the reason and history.

## Authorized Advancements

### Consolidate Four Test Layers and Assess the Workflow Delivery v3 Suite

- **Work carriers:** [Issue #761](https://github.com/hcoona/three/issues/761),
  [Issue #762](https://github.com/hcoona/three/issues/762),
  [Issue #763](https://github.com/hcoona/three/issues/763) and
  [Issue #764](https://github.com/hcoona/three/issues/764), with
  their common delivery and independent review carriers.
- **Authorized outcome:** Consolidate only the four ordered selections below
  from the independently reviewed [#738 analysis](https://github.com/hcoona/three/issues/738).
  Allocate repeated checks to existing owners, preserving consumer obligations,
  unique assertions and actual integration boundaries. Bind exact report-qualified
  proposals, removals, protected owners, neutral targets, module survivors,
  assertion transfers and residuals in the work carriers. The union contains
  47 proposals, 94 conditional removals and eight count-neutral changes across
  31 test modules and one bounded shared helper. Preserve all 314 explicit
  protected cases and complete-module survivors. Counts describe the selection;
  they are not quotas or delivered results. Also perform the bounded read-only
  whole-suite assessment below.
- **Common prerequisite and inputs:** Require actual accepted #756 implementation
  and its #752/#754 lineage. Reconcile accepted source, tree, collection and
  current authorities before this Wave's acceptance and dependent work. The
  four layers are ordered internal checkpoints of one candidate, not separately
  accepted predecessors. Unexpected source, owner or authority drift requires
  refreshed analysis. Follow the current
  [v3 handoff](../src/public/lib/three-workflow-delivery-v3/docs/agent-handoff.md),
  [requirements](../src/public/lib/three-workflow-delivery-v3/docs/requirements.md),
  [repository model design](../src/public/lib/three-workflow-delivery-v3/docs/repository-model-release-unit-mld.md),
  [CI design](../src/public/lib/three-workflow-delivery-v3/docs/ci-qualification-mld.md),
  [release design](../src/public/lib/three-workflow-delivery-v3/docs/release-delivery-mld.md),
  [npm design](../src/public/lib/three-workflow-delivery-v3/docs/hcoona-release-smoke-npm-lld.md),
  [NuGet design](../src/public/lib/three-workflow-delivery-v3/docs/hcoona-release-smoke-github-packages-lld.md)
  and [shared testing principles](engineering/engineering-principles.md#testing).
  Preserve original #738 reports, exact recovery, corrections and independent
  dispositions. The immutable selections are SHA-256
  `ccd2a2ba1a51f6b13d92a27e996a79ff9da64ca454f2b1079aaab7f48820940e`
  and `901ac400739dcb81a925d94aa407338f304dbdf868573c86797e0df6d5ec7464`;
  the reviewed combined schedule replaces their sequential-delivery assumptions
  only. Historical authorship/disposition does not approve this candidate.
  GC-N02 remains withdrawn.
- **Ordered selections:**
    1. **Normal-Live observation and terminal evidence (#761):** Select
       `terminal-review.json:RT-01–02`, `live-observation-review.json:LO-01–04`,
       `release-boundaries-review.json:RC-01–04/RP-01–02` and
       `record-values-review.json:PV-01–02`; neutrals are
       `terminal-review.json:RT-CN-01` and `live-observation-review.json:LO-CN01`.
       Preserve 76 protected cases and the exact 653 conditional module survivors.
       Transfer complete literal shapes, values, ordering, supplemental checks and
       excluded-member assertions into existing documents without deriving new
       identities. Keep the five-key Governance and twelve-key Readback literals,
       five forbidden-field scenarios and separate frozen/slotted owners.
       Retain lower HTTP/tag owners, missing-versions as the prover's unprovable
       sentinel, partial as unknown, and all selected privacy/freshness/byte proofs.
       LO-03 keeps exactly its four selected existing input combinations and IDs.
       RT-CN-01 extracts only zero-action closure; the full-proof helper retains
       real-byte proof and return shape. Stop if removed setup has a required
       side effect. LO-CN01 keeps the historical clock, valid replay, ID 803 and URL;
       clear prior capture and require the exact semantic diagnostic plus newline,
       exit 1 and absent Outcome before retiring four repeated wrong-reference calls.
    2. **NuGet journeys (#762):** Select `root-review.json:RC-05–10` and
       `rr-review.json:R738-RR-02–03`; the neutral is
       `rr-review.json:R738-RR-CN-02`. Preserve 52 protected cases and the exact
       239 conditional module survivors. Transfer original stage assertions before
       materialization, binding, input substitution or effect resets as selected.
       Keep replay rejection, one publication and identical Result bytes before
       finalizer resets; retain all six finalization scenarios. Transfer producer
       assertions only to the existing `omitted=None` row. Preserve all four Live negatives, the operator rerun sentinel and
       operator-only negatives, with the typed actor/attempt/revision diagnostic.
       RR-CN-02 changes local operands in seven membership rows only; retain both
       positive checks and other `native_case` consumers without fixture redesign.
    3. **Model, CI and public control entrypoints (#763):** Select
       `next-root-review.json:NROOT-01–10` and `root-review.json:RC-01–04`;
       neutrals are `ci-record-review.json:CI-R-CN01`,
       `commit3-values-review.json:C3-CN01`, `gc-review.json:GC-N01`,
       `next-root-review.json:NROOT-CN-01` and `rr-review.json:R738-RR-CN-01`.
       Preserve 81 protected cases and the exact 761 conditional module survivors.
       Retain selected-checkout, real installed HK/Git/helper execution, exact
       inventory, independent roots, native/npm routing and scheduler boundaries.
       Keep manual-CI result/rendering/finalizer assertions with 900-second SLO and
       no shadow, plus existing PR thresholds/cohorts. Preserve full canonical
       admission, literal fields and no-provider-reexecution guards. Permit only
       the local deferred seven-row golden selector with fresh records and fixed
       bytes/digests. C3-CN01 removes 19 trailing baseline validations, not cases;
       retain negatives, diagnostics, literal fields, positive and TOCTOU owners.
       The loss of broad sequential post-failure corruption detection remains an
       explicit residual; standalone positives do not reproduce it.
    4. **Bounded readers and acceptance admission (#764):** Select
       `aa-review.json:AA-C01–02`, `gc-review.json:GC-R01`,
       `http-adapters-review.json:HTTP-01–02`, `next-gc-review.json:GC-A01–03`,
       `nuget-profile-review.json:NPF-01` and
       `nuget-read-consumer-review.json:NC-01/NG-01`. Preserve 106 protected cases
       and the exact 865 conditional module survivors. Transfer no-helper-call,
       four-read and schema/redirect assertions to existing owners before removal
       or replay. Keep all selected byte/binding/order, normalization, no-network
       and resource/transport safety owners. NG-01 removes only `api-origin` from
       the bounded HTTP failure test after transferring failed-client/no-new-call,
       token-scan and strict failure-record booleans to its Location-body owner.
       Both distinct-L2 policy and repeated-L1 confidentiality paths stay unchanged.
- **Cross-layer preservation:** Preserve the complete ArtifactReference
  frozen/slotted/mutation/serialization owner and all ten NuGet adapter
  status/error owners across the reader stage, checking decorated function and
  assertion identity as well as nodes. Execute all three adapter 200/201/202
  loopback cases under CPython 3.13.12; skips do not validate RR-03 removals.
  Keep release status=201 composition and both missing/different readbacks.
  Refresh the seven direct observation-helper consumers against actual #756 and
  the final candidate. Preserve their unselected helper bodies and behavior.
- **Validation and independent review:** Transfer unique assertions before
  removing original owners. For each layer retain immutable incremental and
  cumulative patches, parent/output identities, exact node/assertion deltas and
  complete affected/shared-owner/helper-consumer test results. Gate the next
  layer on those checks and resolved author-detected problems. Reconcile the
  complete validation closure, currently 35 modules; newly discovered consumers
  require validation rather than being excluded by that inventory count.
  After all layers, prove the patches reproduce the full candidate from the
  common accepted base, separate additions/renames from removals, and run the
  required normal full v3/repository checks. Create one ordinary implementation
  commit with all hooks enabled. A matching full pre-commit run may supply the
  final local gate; no hook, profile, check or hosted job is waived. Formal
  independent layer, domain and evidence reviews follow successful full local
  validation and precede PR creation. Independently adjudicate material findings;
  refresh affected validation and reviews after changes. Follow
  [agent-execution guidance](engineering/agent-execution.md) and the handoff's
  contraction protocol. Failed ownership assumptions retain original tests and
  require refreshed analysis, never replacement deletions to meet a count.
- **Whole-suite assessment:** Read the resulting v3 tests, relevant production
  boundaries and retained evidence to assess assertion ownership, missing
  scenarios, matrix allocation, fixture isolation, actual boundary execution and
  retained failures. Record evidence and limits before implementation acceptance.
  Findings that invalidate a selected removal or claimed gate block that claim.
  Unrelated implementation findings require a later accepted grant. Inventory
  exhaustion alone establishes neither necessity nor sufficiency of the suite.
- **Scope and effects:** Modify only the exact 31 test modules bound in the
  four Issue selections under `src/public/lib/three-workflow-delivery-v3/tests/`
  and the bounded `release/observation_fixtures.py` extraction. Permit only
  selected assertion transfers/removals/neutrals, the named closure helper and
  local golden selector, `capsys`, and proven newly unused local setup/imports.
  Add no test cases, hidden replay loops, replacement scenarios, shared cache or
  unrelated fixture abstraction. Permit ordinary existing validation/dependency
  operations, task-owned evidence and normal Git/Issue/PR delivery. Preserve
  #721 resource ownership, bounded child lifetimes, resource observations and
  failed/interrupted evidence. Stop on exhaustion or an unreaped task session
  and resolve ownership before continuing. Production, dependency definitions,
  workflows/HK, resource limits, authorities and historical/native evidence
  remain unchanged. Other v3 development, native probes, dispatch/rerun,
  external publication, administrative or authentication/access changes,
  host-wide cleanup and unrelated process termination are excluded. Existing
  npm/NuGet gates and spent-operation boundaries remain in force.
- **Evidence and completion:** Preserve each selection's residuals, including
  retired cross-field/status samples, grouped diagnostics and controlled-input
  limits. Lower owners do not prove exhaustive end-to-end or mutation equivalence.
  Case counts and static setup-call reductions prove no measured runtime or
  peak-inode saving. Retain layer patches, exact deltas, normal validation,
  resource observations, independent reviews, dispositions and whole-suite
  assessment in the work carriers. End the shared finite grant through reviewed
  Wave deletion or replacement after accepted implementation and evidence.
