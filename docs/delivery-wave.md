# Current Delivery Wave

This record is the sole positive work-authorization authority under the
[repository record policy](governance/record-system.md#work-authorization-and-concurrency).
Only its accepted `main` version grants work. Issues, PRs, sessions and proposed
edits cannot add to or enlarge a grant. Merge changes authorization; deletion
ends a grant. Git and the proposing PR retain the reason and history.

## Authorized Advancements

### Consolidate Governance Admission and Source Observation Test Owners

- **Work carrier:** [Issue #754](https://github.com/hcoona/three/issues/754) and its delivery and review carriers.
- **Authorized outcome:** Implement only `eligibility-review.json:EL-02`
  through `EL-05` and `governance-observation-review.json:GG-01` through
  `GG-04` from the independently reviewed
  [#738 analysis](https://github.com/hcoona/three/issues/738). Concentrate shared
  Governance schema/admission rules and source-error/freshness checks in their
  existing owners. Bind the exact removal, affected-case, partial-survivor and
  owner manifests in the work carrier. The selection identifies three whole
  test removals, eight row removals from five matrices, one unused helper and
  three unique assertion obligations transferred into two existing owners.
  Its 11-case reduction is conditional, not a quota or delivered gain.
- **Prerequisites and governing inputs:** Require accepted #749 and #752
  implementations and reconcile their final accepted source, tree, authorities
  and actual retained collection before dependent work. Refresh this selection
  when prerequisite source or relied-on authority changes. Follow the current
  [v3 handoff](../src/public/lib/three-workflow-delivery-v3/docs/agent-handoff.md),
  [requirements](../src/public/lib/three-workflow-delivery-v3/docs/requirements.md),
  [Governance integration design](../src/public/lib/three-workflow-delivery-v3/docs/governance-integration-mld.md),
  [npm design and validation](../src/public/lib/three-workflow-delivery-v3/docs/hcoona-release-smoke-npm-lld.md)
  and [shared testing principles](engineering/engineering-principles.md#testing).
  Retain frozen #738 reports, exact-byte recovery, independent dispositions,
  residuals and GG-SOURCE-ANCHORS correction. Historical status stays historical;
  withdrawn GC-N02 and all unselected neutral suggestions remain excluded.
- **Validation and review:** Transfer EL-05's exact test-primitive suite identity
  and strict live-enabled assertions into the forged-pass owner's original
  already-produced Decision document before it is forged. Preserve blocked
  result/diagnostic checks and exact forged-pass rejection; add no evaluator,
  parser or native call and do not patch the production primitive registry.
  Transfer GG-04's identical freshness-error `issubclass` assertion before
  deleting its standalone owner. Keep all three disabled/expired/changed rows,
  exact raised subtype, literal comparison diagnostic, protected/read order and
  original Eligibility lineage.
  Preserve all 56 explicit owner cases, all retained parameter IDs, both complete
  affected-module survivors and all 15 real Governance Git cases. Keep isolated
  result/diagnostic guards in both admission modes plus static-finding rejection;
  missing inventory and forbidden digest-shim diagnostics; enabled-ready and
  disabled-blocked schema contracts; precise disposable-precondition rejection;
  both genuine ValueError and TypeError semantic observer cases; typed Git
  translation with cause; and original transport-exception identity at each
  retained stage. Keep every real edit/revert, delete/restore, rename, side-branch
  touch, ancestry, object-format, path/mode/history, ambient-isolation and fetch-race
  scenario. Approval, publisher and exact-satisfied each retain their independent
  fresh-source duty.
  Reconcile exact pre/post nodes, three whole functions, five partial functions,
  all original owner assertions and surviving IDs, with renames separate. Run both
  complete affected modules, the unchanged actual Governance Git module and
  required existing full v3/repository checks. Independently review implementation
  and domain/evidence boundaries; independently dispose material findings. Failed
  ownership assumptions retain the original test and require refreshed analysis.
  Report compound-input, enabled-state, selected-ref, opaque-message and grouped
  diagnostic limits without claiming identical inputs, exhaustive mutation
  equivalence, effective permission or native capability. Follow the
  [agent-execution guidance](engineering/agent-execution.md).
- **Scope and effects:** Permit only
  `src/public/lib/three-workflow-delivery-v3/tests/release/test_eligibility.py`
  and
  `src/public/lib/three-workflow-delivery-v3/tests/release/test_commit8_governance_observation_errors.py`
  for the selected changes, ordinary existing validation and dependency/tool
  operations, task-owned evidence and normal Git/Issue/PR delivery. Delete only
  the selected unused `_make_blocked_with_diagnostic` helper after rechecking
  callers; add no fixture, helper, parameter row or replacement execution.
  Preserve #721's temporary-resource ownership, bounded lifecycle and
  failed/interrupted evidence. Existing isolated Git fixtures, loopback and
  test-owned process lifetimes remain ordinary validation. Stop on resource
  exhaustion or an unreaped task session; retain diagnostics and resolve ownership
  before further execution. Production, actual workflows/HK configuration,
  dependencies, resource limits and historical/native evidence stay unchanged.
  Other #738 candidates, other v3 development, native service probes, dispatch/rerun,
  external publication, administrative or authentication/access changes,
  host-wide cleanup and termination outside existing test-owned lifecycles are
  excluded. Existing npm/NuGet gates and spent-operation boundaries remain in force.
- **Completion boundary:** Retain actual node deltas, validation and resource
  observations, independent reviews and disposed findings in the work carrier.
  Do not claim runtime or peak-inode improvements without measurements. End or
  replace this finite grant through a reviewed Wave change after implementation
  and evidence are accepted. Other proposals require their own accepted finite
  authorization.
