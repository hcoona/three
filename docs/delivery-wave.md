# Current Delivery Wave

This record is the sole positive work-authorization authority under the
[repository record policy](governance/record-system.md#work-authorization-and-concurrency).
Only its accepted `main` version grants work. Issues, PRs, sessions and proposed
edits cannot add to or enlarge a grant. Merge changes authorization; deletion
ends a grant. Git and the proposing PR retain the reason and history.

## Authorized Advancements

### Consolidate Simulation Qualification and Observation Test Owners

- **Work carrier:** [Issue #756](https://github.com/hcoona/three/issues/756) and its delivery and review carriers.
- **Authorized outcome:** Implement only `simulation-review.json:SC-01`,
  `SC-CN-01`, corrected `SC-CN-02` and
  `qualification-observation-review.json:QO-01` through `QO-04` plus
  `QO-CN01` from the independently reviewed
  [#738 analysis](https://github.com/hcoona/three/issues/738). Concentrate
  Simulation model/record admission, Qualification disposition and hypothetical
  observation/finalization checks in their existing owners. Bind exact removal,
  transfer-owner, neutral-target, partial-survivor and full-module identities in
  the work carrier. The selection proposes four whole-function removals, six
  transport-row removals and three count-neutral improvements. Its ten-case
  reduction is conditional, not a quota or delivered gain.
- **Prerequisites and governing inputs:** Require accepted #752 and #754
  implementations and reconcile final accepted source, tree, authorities and
  observed collection before dependent work. Refresh the selection if source
  or relied-on authority changes. Follow the current
  [v3 handoff](../src/public/lib/three-workflow-delivery-v3/docs/agent-handoff.md),
  [requirements](../src/public/lib/three-workflow-delivery-v3/docs/requirements.md),
  [Release Delivery design](../src/public/lib/three-workflow-delivery-v3/docs/release-delivery-mld.md),
  [npm representation and validation requirements](../src/public/lib/three-workflow-delivery-v3/docs/hcoona-release-smoke-npm-lld.md)
  and [shared testing principles](engineering/engineering-principles.md#testing).
  Retain frozen reports, exact-byte recovery, independent dispositions, SC-F01
  correction/readback and QO-04's independent domain disposition. Prior advisory
  authorship does not supply independent approval. Keep historical status
  historical and withdrawn GC-N02 excluded.
- **Validation and review:** Transfer returned-model readiness into SC-01's
  existing golden admission owner, keeping full equality, golden bytes/digests,
  Intent admission and exact context/digest operands. Add QO-01's four literal
  failed/incomplete terminal/class anchors before its existing loop and both
  empty-result assertions inside that loop. Add QO-03's literal observer-producer
  assertion immediately after the already-created absent observation and before
  materialization. Preserve existing owner assertions and transfer all eight
  obligations before deleting former owners.
  Keep QO-02's omitted-observation rejection and exact diagnostic. Retain QO-04's
  exact transport `failed-0` and `incomplete-3` IDs, successful precondition and
  anchored disposition-invariant diagnostic. Preserve all eight constructor
  rows, both empty-disposition boundaries and full admitted Decision equality.
  Do not replay removed inputs in new rows or loops.
  SC-CN-01 must reuse the existing Intent/Binding fixtures, preserving both CLI
  normalizations, real canonical files, stable request/byte identities,
  cross-attempt admission and all status/error assertions; other Qualification
  consumers keep their modeled scenario fixture. Corrected SC-CN-02 changes only
  Attempt dictionary insertion-order coupling to the exact three-key set; keep
  product ordering, distinct run identities and the entire fields/slots/frozen
  mutation test unchanged. Preserve its node name. QO-CN01 keeps its single CLI
  invocation and self-consistent report/record inputs, requires the exact
  action-substitution mismatch stderr plus one newline and asserts the two named
  outcome/summary files absent. Add no GitHub-output assertion for paths absent
  from the scenario.
  Preserve all 36 explicit owners and all complete-module survivors. Reconcile
  exact pre/post node sets, retained assertions and fixture dependencies, with
  renames separate. Run all four complete affected modules and required existing
  full v3/repository checks. Independently review implementation and
  domain/evidence boundaries; independently dispose material findings. Failed
  ownership assumptions retain the original test and require refreshed analysis.
  Report synthetic-input, positional-parser, default-argument, grouped-diagnostic
  and modeled-fixture limits without claiming exhaustive mutation equivalence,
  native capability or measured resource savings. Follow the
  [agent-execution guidance](engineering/agent-execution.md).
- **Scope and effects:** Permit only
  `src/public/lib/three-workflow-delivery-v3/tests/release/test_commit6_contracts.py`,
  `src/public/lib/three-workflow-delivery-v3/tests/release/test_commit6_qualification.py`,
  `src/public/lib/three-workflow-delivery-v3/tests/release/test_commit6_transport_cli.py`
  and
  `src/public/lib/three-workflow-delivery-v3/tests/release/test_commit7_observation.py`
  for the selected changes, ordinary existing validation and dependency/tool
  operations, task-owned evidence and normal Git/Issue/PR delivery. Allow only
  the specified local return binding, unused rerun-alias removal, existing
  fixture selection, explicit retained transport rows and diagnostic/output
  checks. Add no helper, fixture definition, logical scenario or replacement
  production call. Preserve #721's temporary-resource ownership, bounded
  lifecycle and failed/interrupted evidence. Existing isolated loopback and
  test-owned process lifetimes remain ordinary validation. Stop on resource
  exhaustion or an unreaped task session; retain diagnostics and resolve ownership
  before further execution. Production, shared fixture definitions, actual
  workflows/HK configuration, dependencies, resource limits and historical/native
  evidence stay unchanged. Other #738 candidates, other v3 development, native
  service probes, dispatch/rerun, external publication, administrative or
  authentication/access changes, host-wide cleanup and termination outside
  existing test-owned lifecycles are excluded. Existing npm/NuGet gates and
  spent-operation boundaries remain in force.
- **Completion boundary:** Retain actual node deltas, validation and resource
  observations, independent reviews and disposed findings in the work carrier.
  Do not claim runtime or peak-inode improvements without measurements. End or
  replace this finite grant through a reviewed Wave change after implementation
  and evidence are accepted. Other proposals require their own accepted finite
  authorization.
