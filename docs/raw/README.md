# Raw Assets

Store immutable supporting assets here.

Examples:

- downloaded images;
- PDFs;
- datasets;
- exports referenced by files in `../sources/`.

The agent may read from this directory, but it should not modify existing files
unless the user explicitly requests it.
