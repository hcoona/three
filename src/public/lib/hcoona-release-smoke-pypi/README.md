# hcoona-release-smoke-pypi

`hcoona-release-smoke-pypi` is a minimal Python package used only to validate the Three monorepo PyPI release workflow path.

It exercises build-system-integrated NBGV versioning, wheel builds, source distributions, GitHub Release assets, and PyPI publication against a dedicated smoke package name.
