# hcoona-release-smoke-nuget

`hcoona-release-smoke-nuget` is a minimal package used only to validate the Three monorepo nuget release workflow path.

It is intentionally not a general-purpose end-user library.
