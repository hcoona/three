[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "three-workflow-release-contracts"
version = "0.1.0"
description = "Frozen JSON contract models for the Three workflow release design."
readme = "README.md"
requires-python = ">=3.12"
authors = [{ name = "Shuai Zhang", email = "zhangshuai.ustc@gmail.com" }]
license = "LGPL-3.0-or-later WITH LGPL-3.0-linking-exception"
keywords = ["release", "workflow", "contracts"]
dependencies = []

[dependency-groups]
dev = ["pytest>=8.3.4"]
