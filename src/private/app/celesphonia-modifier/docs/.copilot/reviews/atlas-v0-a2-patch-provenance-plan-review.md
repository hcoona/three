# Atlas V0 A2 Patch-Provenance Plan Review

**Increment:** A2 patch-provenance amendment plan

**Outcome:** Implementation ready

**Final independent result:** `No findings`

**Plan commit:** `34973efaf9fd3720c76ed145606effdd679174a9`

**Plan tree:** `2c517358f6094ccfe98128f9953766ea6644b785`

**Initial amendment commit:** `d84cbd10047a87146c3821254e774d4f66a83c78`

**Pre-amendment baseline:** `9edbd57b4f44e76de321e06be81a581ed11b0017`

**A1 release record:** `cdde3a0427765c9f2b969e3e678550e4f7d78edb`

**Historical A2 plan-review record:** `984a1375d510f4a293b4cf0c5984cffc99178fb1`

**Governing plan:** `../plans/atlas-v0-a2-patch-provenance-amendment.md`

**Record reviewer:** `a2-patch-plan-record-review`

## 1. Exact-plan binding

The final independent review examined the complete plan commit and tree above relative to the
pre-amendment baseline. The plan commit equals the shared branch upstream, its tracked worktree was
clean, and the cumulative no-renames candidate changed exactly the four paths in section 2.

The A1 release-record dependency was verified:

- `cdde3a0427765c9f2b969e3e678550e4f7d78edb` is reachable from the plan commit;
- its first parent is `4fa96f57d9834b67a9947aaf251384558aae6d22`; and
- it adds only `docs/.copilot/reviews/atlas-v0-a1-release-gate.md`.

The pre-amendment baseline is the historical A2 tool-safety record. It remains immutable evidence,
but the governing amendment revokes its private-run authority.

The commit containing this record must:

1. use the plan commit as its first parent;
2. change only this plan-review record;
3. contain the independently reviewed staged blob unchanged;
4. be pushed to the shared branch; and
5. pass the parent, path, blob, upstream, and clean-worktree checks in the governing plan.

Any other repository change or record-blob change creates another plan candidate and invalidates
this review. The verified record commit becomes implementation diff base `B`.

## 2. Reviewed plan candidate

The cumulative no-renames candidate from the pre-amendment baseline changed exactly:

- `../README.md`;
- `../plans/atlas-v0-a0-research-contract.md`;
- `../plans/atlas-v0-a2-intake-safety-plan.md`; and
- `../plans/atlas-v0-a2-patch-provenance-amendment.md`.

The final reviewer also read:

- repository-root, project, and documentation `AGENTS.md` files;
- `../plans/project-operating-model.md`;
- `atlas-v0-a1-release-gate.md`;
- `atlas-v0-a2-plan-review.md`;
- `atlas-v0-a2-tool-safety-review.md` as historical evidence only;
- the existing Atlas library, CLI, tests, projects, lock files, and schemas named by the plan; and
- the complete cumulative diff, not only the last correction.

No private or untracked input was part of review.

## 3. Material decision and scope

The accepted amendment binds the released A0 corpus to Steam application `1786790`, public build
`13624401`, the official patch product URL, version label `CN Patch v1.05.2`, and a private
held-handle SHA-256 of the exact retained installer used for A0.

The installer path and hash remain private. Package identity does not prove installed-file identity.
Fresh human installation attestations and fresh package rehashing govern discovery, confirmation,
and copy. Ordinary gameplay save writes do not invalidate the installation attestation.

The accepted trust profile remains `trusted-local-filesystem/v1`. The amendment authorizes C#/.NET
10 implementation and synthetic validation only. Private discovery remains blocked until amended
source candidate `S` and record-only tool-safety child `T` pass their separate gates.

## 4. Reviewer independence

Every iteration used a dedicated clean-context subagent that did not author the candidate. No
reviewer model override was used.

| Iteration | Reviewer role                           | Candidate  | Result         |
| --------: | --------------------------------------- | ---------- | -------------- |
|         1 | Independent clean-context plan reviewer | `d84cbd10` | Five findings  |
|         2 | Independent clean-context plan reviewer | `2ee899e0` | Seven findings |
|         3 | Independent clean-context plan reviewer | `3e8ebff4` | Five findings  |
|         4 | Independent clean-context plan reviewer | `43bfe3a3` | Two findings   |
|         5 | Independent clean-context plan reviewer | `6e73d63d` | Seven findings |
|         6 | `a2-plan-review-6`                      | `26deb15f` | Six findings   |
|         7 | `a2-plan-review-7`                      | `4662b7ab` | Four findings  |
|         8 | `a2-plan-review-8`                      | `4c282398` | One finding    |
|         9 | `a2-plan-review-9`                      | `7c72b3a5` | One finding    |
|        10 | `a2-plan-review-10`                     | `880ef403` | One finding    |
|        11 | `a2-plan-review-11`                     | `34973efa` | `No findings`  |

The final reviewer examined the complete four-path candidate and governing sources. The independent
record reviewer named above must examine this exact staged record blob before publication.

## 5. Finding disposition

All 39 findings from iterations 1 through 10 were resolved, committed, pushed, and reviewed again
before the final complete review.

### Iteration 1

- Added executable C# preparation rather than relying on a prose-only private request.
- Excluded every prior v1 artifact from v2 recovery.
- Separated full provenance, package-hash identity, and alias equality.
- Preserved single-result cleanup semantics.
- Narrowed installation attestation to patch-managed executable and definition content.

### Iteration 2

- Excluded later or different patch packages from the accepted source profile.
- Replaced private path operands with explicit public repository-root and survey-alias arguments.
- Completed CLI grammar, help, and fixed-output behavior.
- Prevented recovery from authorizing another survey identity.
- Added preparation-input custody and the complete public Git evidence chain.
- Required durable repository-safe private-run evidence.

### Iteration 3

- Added explicit human attestation of the official source and version.
- Separated hash and alias equality chains.
- Defined the exact C# direct-apphost driver and root-map lineage.
- Brought every amendment line within the 100-character gate.

### Iteration 4

- Added immutable custody for rejected and abandoned request decisions.
- Added durable human request-review and private-output-audit evidence.

### Iteration 5

- Moved state-bound receipts into canonical survey-relative paths.
- Bound staged live-derived outputs before the root-map marker.
- Added immutable review decisions and explicit abandonment custody.
- Required phase-fresh installation attestation.
- Limited post-source documentation changes to exact record-only paths.
- Restored the original A2 intake-approval fields.
- Closed the preparation- and review-receipt schemas.

### Iteration 6

- Made release evidence compare with exact required literals, including required `false` values.
- Superseded every conflicting original request, count, invocation, and preflight clause.
- Required a fresh source-bound apphost rebuild before private commands.
- Standardized every receipt binding on full survey-relative paths.
- Restored copy-plan audit and explicit privacy assertions.
- Added the missing review-command global-help usage.

### Iteration 7

- Superseded the affected original copy, publication, recovery, and lifecycle clauses.
- Bound the complete approved discovery custody quartet into the root-map recovery marker.
- Added six-row terminal custody for rejected and abandoned decisions.
- Added bounded raw-stream validation and native exit-code forwarding through a fixed launcher.

### Iteration 8

- Added the exact terminal inventory staging path.
- Defined `File.Replace`, backup, final-marker, and every terminal crash-recovery state.

### Iteration 9

- Superseded the original implementation, schema, record, comparison, and stage identities.
- Preserved every unaffected validation, privacy, review, and invalidation rule.

### Iteration 10

- Aligned the `.copilot` index with required public identifiers and commit bindings in the
  private-run acceptance record.

### Iteration 11

The reviewer returned exactly `No findings`.

## 6. Acceptance evidence

The accepted plan:

- freezes the existing A0 roots, counts, rules, decisions, and one-shot survey identity;
- defines closed v2 request, root-map, state, copy-receipt, review, and terminal-custody contracts;
- binds private package identity without claiming installed-file identity;
- defines phase-fresh attestation and package rehashing before live-source operations;
- gives rejected and abandoned decisions a terminal marker and state-safe inventory custody;
- makes root-map publication the no-source-reopen discovery recovery marker;
- preserves state revision 3 as the sole qualified-copy signal;
- defines exact CLI help, fixed launcher bytes, diagnostics, exit codes, and privacy behavior;
- requires a fresh source-bound apphost rebuild before every private-phase command;
- fixes every implementation, test, schema, documentation, and record path;
- preserves the BCL-only production and existing project/dependency boundaries;
- defines exact `P -> B -> S -> T -> A -> R -> G` parent and record-only path order;
- requires human review of every private request and output; and
- keeps private discovery blocked until the amended source-safety record is verified.

## 7. Validation evidence

The following checks passed on the exact pushed plan candidate:

- the candidate descends from the pre-amendment baseline and exact A1 release record;
- the commit and tree match the identifiers above;
- the commit equals the shared upstream and the tracked worktree is clean;
- the cumulative no-renames diff contains exactly the four paths in section 2;
- every incremental correction stayed within that cumulative path boundary;
- Prettier, Markdownlint, EditorConfig, typo, and commit-message hooks passed;
- `git diff --check` passed;
- committed amendment lines and all cumulative added/modified lines are at most 100 characters;
- committed Markdown is LF-only; and
- the final clean-context reviewer returned exact `No findings`.

No .NET implementation, schema, dependency, project, SDK, or toolchain file changed in the plan
candidate.

## 8. Private-evidence statement

Amendment planning accessed no installed game file, live save, private workspace, installer bytes,
installer metadata, generated request, private manifest, private hash, source name, or source text.
This record contains only public identifiers, repository-safe aggregates, Git bindings, and review
evidence.

## 9. Execution decision

A2 amendment implementation may begin only after this exact record blob receives independent
`No findings`, is committed unchanged as the only child of the plan commit, is pushed, and passes
section 1 verification.

The verified record commit becomes `B`, the sole implementation diff base. Implementation uses
synthetic data only and may not inspect the installed game, live saves, `.private`, installer, or
any private A0/A2 artifact. Private request preparation, hashing, discovery, confirmation, copy, and
preflight remain prohibited until their later gates in the governing plan pass.
