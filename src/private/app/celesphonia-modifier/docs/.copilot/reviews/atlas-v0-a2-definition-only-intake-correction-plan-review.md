# Atlas V0 A2 Definition-Only Intake Correction Plan Review

**Lifecycle:** Proposed plan-review evidence before verified shared `R14`

**Increment:** A2R14 - Definition-Only Intake Correction

**Outcome:** Implementation authority is permitted only after release; protected authorization-control
and private evidence operations remain blocked

**Final independent result:** `No findings`

**Base G0R7:** `d31e8e7d0782cff72c1f4f08c35146622893da89`

**Historical G13:** `f4785dba8cd3a286af08ed804361a27c3a76144f`

**Final P14:** `b3e8da834277ffaf4c96b066fc69322549a1af5d`

**Final P14 tree:** `1cd2685d547e755d52cb8fb843380d2f183a6da6`

**Governing plan:** `../plans/atlas-v0-a2-definition-only-intake-correction.md`

**Governing plan blob:** `5f193154d48a9af273117ecf95d22fff0b71f803`

**Governing plan SHA-256:**
`2e23aaeac406175d68841e9c3cf56c6e14965d082cfbc7b41b94d922a9aabb88`

**Planned staged-record reviewer:** `a2r14-plan-record-reviewer`

## 1. Exact plan candidate

`P14` is the direct child of exact `G0R7`. Its exact no-renames path set is:

```text
src/private/app/celesphonia-modifier/docs/.copilot/
  README.md
  plans/
    atlas-v0-a0-approved-definition-authority-correction.md
    atlas-v0-a2-clean-workspace-rebuild.md
    atlas-v0-a2-definition-only-intake-correction.md
    atlas-v0-a2-intake-safety-plan.md
    atlas-v0-execution-plan.md
    save-semantic-atlas-plan.md
```

Its exact blobs are:

```text
README.md
  3c91af793bb3abf18af941c012c998efa6b431ec
atlas-v0-a0-approved-definition-authority-correction.md
  1dbefe817a8bed2b9a9535385891a515ac7479e7
atlas-v0-a2-clean-workspace-rebuild.md
  8b8921b7ae0a4f2049467edd6633ebcd24c1156c
atlas-v0-a2-definition-only-intake-correction.md
  5f193154d48a9af273117ecf95d22fff0b71f803
atlas-v0-a2-intake-safety-plan.md
  800e7c6d04a7e1399d589c65eeea59b3c0d4de6d
atlas-v0-execution-plan.md
  df54c4c476bd33e77f132a0ca96b8b5c824a9664
save-semantic-atlas-plan.md
  7f1b8bb43ca802611400eb86f3db5f60015b314d
```

`P14` was committed unchanged from the final reviewed candidate, pushed, and verified as the clean
shared development-branch tip before this record was authored.

## 2. Review iterations and independence

Every reviewer was independent of plan authorship and received only repository-safe tracked
instructions, plans, records, released source, Git facts, and the complete staged candidate. No
reviewer received a historical request or authority, protected authorization, runtime locator, game
tree, private workspace, inventory, hash, path, count, mapping, source content, or execution result.

| Candidate                  | Reviewer                         | Result                  | Adjudication |
| -------------------------- | -------------------------------- | ----------------------- | ------------ |
| Initial six-file candidate | `p14-plan-reviewer`              | 3 medium                | 3 TP, 0 FP   |
| First corrected candidate  | `p14-plan-rereviewer`            | 1 high, 3 medium        | 4 TP, 0 FP   |
| Historical-request finding | `historical-request-adjudicator` | TP concurrence          | Confirmed    |
| Second corrected candidate | `p14-plan-reviewer-3`            | 1 high, 2 medium        | 3 TP, 0 FP   |
| Third corrected candidate  | `p14-plan-reviewer-4`            | 1 high, 2 medium        | 3 TP, 0 FP   |
| Fourth corrected candidate | `p14-plan-reviewer-5`            | 2 high, 1 medium, 1 low | 4 TP, 0 FP   |
| Fifth corrected candidate  | `p14-plan-reviewer-6`            | 2 high, 3 medium        | 5 TP, 0 FP   |
| Final seven-file candidate | `p14-plan-reviewer-7`            | `No findings`           | Not needed   |
| Post-hook exact candidate  | `p14-plan-reviewer-8`            | `No findings`           | Not needed   |

The historical-request adjudicator independently confirmed that the issue was not missing
authentication of every legacy request field. The defect was promotion of historically inert request
members into authority. The final plan retains only the A0R6 four-field historical anchor and makes
the live definition root a separate protected project-leader authorization.

## 3. Planning-drift gate

Each review round rechecked the accepted outcome, claim, scope, exclusions, trust model, evidence,
authority, and handoff before correction. The final ideal minimum is:

- retain exact `G0R7` stored definition mapping as historical authority without another private replay;
- read the fixed historical request only through its A0R6 four-field consequential boundary;
- derive the fixed historical authority path from canonical layout, not a historically inert field;
- grant one live definition-root locator separately in an exact protected authorization;
- exclude saves and `Game.exe` before direct metadata access or recursion;
- reconcile the complete live definition denominator before any definition-content read;
- copy only included definitions through held handles into one fresh protected lineage;
- qualify only through exact state r2 and per-file receipt evidence;
- use one durable final or not-granted authorization path and no undefined marker protocol;
- bind the exact tracked source, protected harness source, and every selected managed execution file;
- preserve A1 behavior and completed A0R6, A0R7, and A2R13 facts;
- keep implementation, control, private-read, A3, WinUI, network, and original-write authority
  separated by their exact gates; and
- preserve a resumable Git topology with no authority inferred from worktree presence.

The corrections narrowed contradictions and made existing requirements executable. They did not add a
save workflow, hostile-local claim, compatibility path, transformed authority, changed-input retry,
definition lifecycle, replacement A3 plan, or original-data write.

## 4. TP/FP adjudication

All 22 independent review findings were atomic, in scope, and adjudicated TP. There were zero FP
dispositions.

### Round 1

1. The blanket source-lock exclusion contradicted the required held handle. The final scope permits
   only per-file denial of write and delete sharing while copying one included definition.
2. The save and executable exclusions contradicted unavoidable parent-enumeration name observation.
   The final boundary permits only immediate candidate-name observation for pruning, with no direct
   probe, attribute read, child traversal, content read, or output.
3. The two new A2R14 commands were described as the only tracked commands. The plan now preserves the
   complete A1 `empty-survey` contract, help surface, behavior, and tests.

### Round 2

1. Historically inert request members were promoted into authority. The final ingress uses only
   `schemaVersion`, `expectedBaselineSha256`, `expectedSteamAppId`, and `expectedBuildId`; the live
   definition root is granted separately.
2. Recovery referred to terminal and phase markers absent from the complete schema and layout. The
   final design uses r1, receipt, r2, deterministic publication, and governance only.
3. The predecessor A2 plan described completed A0R6 as planning-only and omitted G0R7. Its facts and
   resume procedure now bind completed G0R6 and G0R7.
4. The protected harness had no durable source path, complete file set, or invocation surface. The
   final plan fixes its two-file source root, build outputs, actions, and no-rebuild rule.

### Round 3

1. Candidate rendering required fresh-target absence checks outside its control authority. The final
   boundary permits only bounded BCL metadata checks without ancestor enumeration or descendant access.
2. A post-decision output failure conflicted with terminal precedence. The final design uses an atomic
   unchanged-byte not-granted path; later output failure cannot reclassify that durable branch.
3. The fresh root prefix and workspace relation were undefined. The final formulas derive exactly one
   `.private/atlas-v0-a2r14-<run-id>/survey-000001` workspace.

### Round 4

1. `not-granted` had no durable evidence. The unchanged candidate now atomically promotes to the
   canonical not-granted path; failed validation or promotion is safe refusal.
2. Framework-dependent execution bound only DLLs. The final release and authorization bind every
   selected DLL, deps file, and runtimeconfig file and invoke them through explicit exec semantics.
3. Candidate creation required an authorization directory whose creation was not permitted. The
   control boundary may create exactly that directory under a validated `.private` parent.

### Round 5

1. G14 was required to record its own future commit identity. The final record binds exact C14 and
   ancestry; G14 identity is established externally after commit and then bound by authorization.
2. CLI and harness dependency resolution consumed separate app-local Atlas DLL copies. Both exact
   paths are recorded and must equal the same `atlasAssemblySha256`.
3. One predecessor banner still called A0R6 planning-only. It now records completed G0R6 and G0R7.
4. The index omitted A0R7 plan-review and completion evidence. Both exact records are now indexed.

### Round 6

1. The bounded fixed-request read lacked authority for its required path, drive, and reparse checks.
   The control boundary now permits only those fixed-request metadata checks before reading bytes.
2. The harness test suite had no permitted action. The exact isolated `self-test` action uses only one
   caller-owned empty temporary root and has a fixed repository-safe output.
3. The ingress named active output fields instead of historical request fields. It now validates
   `expectedSteamAppId` and `expectedBuildId` before projecting active identifiers.
4. Exact .NET commands bypassed the retained tool policy. Every shell command now uses
   `mise exec -- dotnet exec`.
5. A2R13 still declared itself active and blocked after G13. Its plan, index, P14 path set, and
   handoff now record completed historical status with no retry.

The commit gate later found four overlong command lines. They were equivalently wrapped with
PowerShell continuation markers, markdownlint passed, and the complete changed candidate received a
new independent `No findings` review. The first commit attempt then failed only commit-title casing;
the staged candidate did not change.

## 5. Validation evidence

Repository hooks validated exact `P14` Markdown with EditorConfig, typos, markdownlint-cli2, and
Prettier. Commitlint accepted the final AP Title Case subject. `git diff --check` passed. Git
verification proved:

- exact `G0R7` is `P14`'s direct parent;
- `G0R7..P14` changes exactly the seven paths and blobs in section 1;
- committed `P14` tree equals the final reviewed candidate;
- `P14` equals the remote development-branch tip; and
- the worktree was clean before record authorship.

No build, harness test, historical input read, protected authorization operation, live-tree access,
fresh-workspace creation, discovery, copy, or private evidence operation occurred during planning and
review.

## 6. Accepted boundary

After verified shared `R14`, work may implement only the exact definition-only C14 path and protected
harness source defined by the governing plan. It may run repository-safe tracked tests and the
synthetic protected harness self-test, freeze the exact implementation candidate, and obtain release
review.

Neither `P14` nor `R14` authorizes rendering an authorization candidate, reading the fixed historical
request, reading the historical authority, accessing a live definition tree, creating a fresh private
workspace, copying definition content, beginning A3, implementing WinUI, using network or telemetry,
or writing original data. Those operations remain gated by exact G14 and, where required, the exact
promoted final protected authorization.

## 7. R14 release gate

This proposed record grants only implementation authority after release. Work may continue only after:

1. this exact staged record receives independent `No findings`;
2. it is committed unchanged as `R14`, the direct child of exact `P14`;
3. `P14..R14` adds only this record path;
4. the committed record blob equals the reviewed staged blob;
5. `R14` is pushed and verified as the clean shared branch tip; and
6. no C14 implementation or protected/private operation begins before verified shared `R14`.
