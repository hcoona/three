# Atlas V0 A6 Gold Mutation Kernel Plan Review

**Lifecycle:** Proposed activation evidence before verified shared `R6R3`

**Final independent plan result:** `No findings`

## Decision

The corrected P6R3 plan is accepted for activation only after this exact record receives staged-record
`No findings`, is committed unchanged as the sole child path of exact final P6R3, is pushed, and is
verified as shared `R6R3`. The planned staged-record reviewer is
`a6r3-plan-record-final-reviewer`.

## Provenance

- Base X6R2:
  `85db738d466dbaa918683bb8e4c56775c5e7544f`
- Initial P6R3:
  `8d553062bdb8ca3ad46ee992301e30c19a7865a8`
- Final corrected P6R3:
  `fe21d472d4ea14a8c15b92a3bb13dbfe62adf865`
- P6R3 tree:
  `f4c63e94b74892f2d90e9b87972e9104ecae6de1`
- Plan:
  `../plans/atlas-v0-a6-gold-mutation-kernel.md`
- Plan blob:
  `7b60f5619640c0a6310a0c2208c80369d4a3645d`
- Plan SHA-256:
  `e0b9d515a256dac3e0c8d1ff2368024383bd8835252fb19117d4812bd4ea944b`
- README blob:
  `74fb5532459931cf9b784478633d17cae750b889`
- README SHA-256:
  `e058893ace49c23d86dd25ce628e859adfd6f1e268580be891bb8de25e33d8ce`
- Exact P6R3 path set:
  modified
  `src/private/app/celesphonia-modifier/docs/.copilot/README.md` and added
  `src/private/app/celesphonia-modifier/docs/.copilot/plans/atlas-v0-a6-gold-mutation-kernel.md`
- Exact P6R3 is the pushed shared development-branch tip from which this record was authored.

## Independent Review

All reviewers were fresh independent general-purpose GPT-5.6 agents and did not author the candidate.
Eight findings were adjudicated as true positives, with zero false positives:

| Candidate            | Reviewer                        | Result        | Adjudication |
| -------------------- | ------------------------------- | ------------- | ------------ |
| Initial candidate    | `a6r3-plan-reviewer`            | 4 findings    | 4 TP, 0 FP   |
| Corrected candidate  | `a6r3-plan-final-reviewer`      | 1 finding     | 1 TP, 0 FP   |
| Corrected candidate  | `a6r3-plan-gate-reviewer`       | 1 finding     | 1 TP, 0 FP   |
| Published candidate  | `a6r3-plan-no-findings`         | `No findings` | Not needed   |
| First record draft   | `a6r3-plan-record-reviewer`     | 1 finding     | 1 TP, 0 FP   |
| Corrected cumulative | `a6r3-plan-correction-reviewer` | `No findings` | Not needed   |
| Corrected record     | `a6r3-plan-record-rereviewer`   | 1 finding     | 1 TP, 0 FP   |

The accepted corrections:

1. removed nondeterministic phase-specific cancellation tests and retained deterministic kernel
   evidence plus existing codec and reader cancellation authority;
2. removed an impossible direct scalar-reference parser case while retaining equal-span helper
   evidence;
3. defined invalid-span precedence before overlap classification;
4. simplified bounded strict UTF-8 conversion to one decode rather than unnecessary chunk-boundary
   machinery;
5. aligned decompressed-limit handling with the released
   `MaximumDecompressedCodeUnits` contract; and
6. required null and invalid reader-limit validation before mutation classification; and
7. removed the last contradictory chunked-decode cancellation sentence and permitted reviewed plan
   corrections to descend while preserving the exact cumulative two-path planning boundary; and
8. expressed the cumulative P6R3 path set with exact full repository-relative paths.

The complete final corrected two-document candidate and every disposition then received exact
`No findings` before this corrected record was staged.

Plan authoring and review used only tracked, repository-safe synthetic content. No private receipt,
snapshot, save, survey, definition, installation, ignored artifact, candidate value, or private
structural count was accessed.

## Accepted Boundary

Verified shared `R6R3` authorizes only a repository-safe synthetic implementation that:

- reuses one fixed A6 inspection for `party._gold` and `variables._data[215]`;
- returns exact original compressed bytes for semantic no-op;
- replaces only one or two validated scalar UTF-8 spans for changed candidates;
- preserves every unrelated UTF-8 byte;
- compresses with the released game codec;
- re-reads under the caller-supplied reader limits and verifies exact bytes and A6 consistency;
- returns immutable owned compressed bytes through a closed result and failure surface; and
- changes exactly the four implementation paths named by the governing plan.

It adds no CLI, filesystem, stream or path API, private execution, gameplay range, semantic
authority, general patcher, writer, backup, transaction, recovery, persistence, or WinUI.

## Validation

The exact plan candidate passed applicable HK formatting, Markdown, spelling, EditorConfig, and
`git diff --check` validation. Commit hooks and commitlint passed for the initial and corrective
P6R3 commits. Git verification proved:

- exact X6R2 is the initial P6R3's direct parent;
- the final corrected P6R3 descends only through the accepted plan correction;
- `X6R2..P6R3` changes exactly the two planned paths;
- the P6R3 tree, plan blob, README blob, and SHA-256 values above are exact; and
- P6R3 was pushed before this record was authored.

No product code, build, private input, filesystem mutation API, or game-save mutation occurred
during planning.

## Activation

R6R3 records this acceptance and adds only this review path as a direct child of exact P6R3. After
staged-record review by `a6r3-plan-record-final-reviewer`, this exact record must be committed
unchanged, pushed, and verified as the shared branch tip before R6R3 activates initial synthetic
C6R3 directly from R6R3.

Accepted C6R3 corrections may descend from that initial candidate only while the cumulative
implementation diff from R6R3 remains exactly the governing plan's four paths. Verified shared G6R3
releases only pure in-memory candidate generation and grants no filesystem-write or operation
authority.
