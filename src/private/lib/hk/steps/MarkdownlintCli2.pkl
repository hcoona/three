import "../Config.pkl"

markdownlint_cli2 = new Config.Step {
  batch = true
  glob = List("*.md")
  stage = List("*.md")
  check = "markdownlint-cli2 {{files}}"
  fix = "markdownlint-cli2 --fix {{files}}"
}
