[project]
authors = [
  {name = "Shuai Zhang", email = "zhangshuai.ustc@gmail.com"},
]
description = "像调用本地 API 一样调用 chrome-mcp-server 的 Python 客户端，基于 fastmcp client 实现。"
name = "chrome-mcp-client"
requires-python = ">=3.12"
version = "0.0.0-dev"
readme = "README.md"

dependencies = [
  "fastmcp>=2.9.2",
  "typer>=0.16.0",
]
